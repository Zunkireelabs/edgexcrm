import type { AiConfig } from "../../_types";

/**
 * Per-industry AI configuration for real_estate (CRE capital-raise).
 *
 * Phase 1 stub — no prompts/tools wired yet. AI touchpoints (raise
 * copilot, investor-matching, IR summarization) are explicitly deferred.
 * Kept as a scaffold so the manifest can reference `ai: aiConfig` the
 * same way the other industries do.
 */
export const aiConfig: AiConfig = {};
