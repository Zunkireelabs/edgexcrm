import { FEATURES, INDUSTRIES } from "../../../_registry";
import type { FeatureMeta } from "../../../_types";

export const itineraryMeta: FeatureMeta = {
  id: FEATURES.ITINERARY,
  industries: [INDUSTRIES.TRAVEL_AGENCY],
};
