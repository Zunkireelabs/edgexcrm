/**
 * Stub for education-consultancy AI agent configuration.
 *
 * Future work lands an industry-specific system prompt + tools here
 * (e.g. application-status-checker, college-recommender, document
 * collector). Today this exists as the architectural slot — the
 * manifest references `ai` but no real config is wired yet.
 */

import type { AiConfig } from "../../_types";

export const aiConfig: AiConfig = {};
