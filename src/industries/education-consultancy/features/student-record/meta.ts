import { FEATURES, INDUSTRIES } from "../../../_registry";
import type { FeatureMeta } from "../../../_types";

export const studentRecordMeta: FeatureMeta = {
  id: FEATURES.STUDENT_RECORD,
  industries: [INDUSTRIES.EDUCATION_CONSULTANCY],
};
