// Canonical public domain for externally-handed-off campaign URLs — must stay edgex
// regardless of which env/host the admin views the CRM from.
export const CAMPAIGN_PUBLIC_BASE_URL = "https://edgex.zunkireelabs.com";

export const DEFAULT_LEADERBOARD_FIELDS = [
  { key: "study_abroad_interest", label: "Study Abroad Interest" },
];
