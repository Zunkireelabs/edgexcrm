# Feature Catalog

> Human-readable view of every feature in the codebase, where it lives, and which industries use it. Auto-generation script is a future improvement; for now this is maintained by hand alongside `src/industries/_registry.ts`.

Last updated: 2026-06-10 (**NEW INDUSTRY `travel_agency`** — first tenant Arya Travels, mig `043`; itinerary/quote builder + Trip Inquiry panel + Packages catalog + Itineraries list, on branch `feature/travel-agency-industry`, NOT yet merged to stage. Package-of-interest on leads = next build. Prior: 2026-06-04 PM Home view + Orca UI + Org Structure ALL PROMOTED TO PRODUCTION — `main` @ `886f541`. New rows: `home` (personal HubSpot-style landing, `/home` default route, mig `032` standalone tasks + `/api/v1/my-tasks`), `org-structure` (mig `031`), `orca-ui` (UI-only). All earlier features also live in prod: Positions/RBAC, Knowledge Bases, Notifications+badges, Email 1–3, UTM. `stage` leads `main` by docs only.)

---

## How to read this

Every feature is classified as one of:
- **Global** — used by every tenant regardless of industry. Lives outside `src/industries/`.
- **Industry-aware** — used by every tenant but adapts labels/behavior per industry.
- **Industry-scoped** — only available to tenants in the listed industries. Lives in `src/industries/<id>/features/`.
- **Shared** — used by multiple industries but not all. Lives in `src/industries/_shared/features/`.

When adding or moving features, update this table and the constants in `src/industries/_registry.ts`. CLAUDE.md § Industry Scoping Rules has the decision tree.

---

## Global

| ID | Location | Notes |
|---|---|---|
| `home` | `src/app/(main)/(dashboard)/home/` + `src/components/dashboard/home/` + `src/app/(main)/api/v1/my-tasks/` | **Universal** personal landing (shipped + promoted 2026-06-04 PM, `e5446f3` + fixback `e8cbf65`). HubSpot-style single-column `/home` — now the **default post-login route** (root redirect + auth-callback default → `/home`; `/dashboard` stays as the analytics view). First item in `UNIVERSAL_NAV_TOP` (House icon); **always-allowed** in `navAllowed` so restricted positions keep a landing page. SSR Server Component `Promise.all`-fetches per-user widgets via new `queries.ts` helpers (`getMySchedule`/`getMyTasks`/`getMyEmailSnapshot`/`getRecentNotifications` + reused `getLeads({restrictToSelf})`): **My Schedule** (upcoming + overdue meetings/calls from `lead_activities`, `user_id`-scoped), **My Tasks** (personal to-dos, Open/Completed toggle, inline complete + "+ New Task"), **My Leads** (assigned to me, unread-first), **Email snapshot** (unread inbound — **education-only**, auto-hides for it_agency), **Recent Activity** (the user's `notifications`). **Standalone tasks** (mig `032`): `tasks.project_id` nullable + `lead_id` FK + RLS relaxed to own-task-OR-admin, so personal tasks exist independent of a project (education had no projects → no tasks before). The project-board task routes (`/api/v1/tasks*`, `FEATURES`-gated + `requireAdmin`) are untouched; Home uses a separate **universal owner-scoped** `/api/v1/my-tasks` (GET own + POST create; `[id]` PATCH/DELETE check `assignee_id === auth.userId` — 404 cross-tenant, 403 non-owner). Project-board `GET /api/v1/tasks` filters `project_id IS NOT NULL` so personal tasks stay out of the `/projects` workspace. All `scopedClient`; reads SSR, writes optimistic. |
| `leads` | `src/app/(main)/(dashboard)/leads/` + `src/components/dashboard/leads-table.tsx`, `lead-detail.tsx` | All tenants. |
| `pipeline` | `src/app/(main)/(dashboard)/pipeline/` + `src/components/pipeline/` | Multi-pipeline support. |
| `team` | `src/app/(main)/(dashboard)/team/` + `src/components/dashboard/team-management.tsx` | Invites (by position), roles, removals, inline position editor. **Sidebar label is now "Org Structure"** (route stays `/team` — the href is an RBAC nav key in every position's `permissions.nav`, so renaming it would force a JSONB migration for zero benefit). `team-management.tsx` is preserved unchanged as the **Manage** view embedded inside Org Structure (see the `org-structure` row). |
| `org-structure` | `src/app/(main)/(dashboard)/team/` + `src/components/dashboard/org-structure/` + `src/app/(main)/api/v1/org-layers/` | **Universal** persistent layered org chart (shipped 2026-06-04, `3a7bc86`). Renames the Ops "Team" nav item → "Org Structure" (icon `Network`; route stays `/team`). **Model**: `org_layers` (ordered, tenant-scoped, mig `031`) **contain** `positions` (new nullable `positions.layer_id` FK, `ON DELETE SET NULL`); each position **aggregates** its real members (`tenant_users.position_id` rollup). Cards = positions, not people. **Four views**: **Editor** (persisted layer + position CRUD/reorder; "Add Role" = create a position in that layer), **Hierarchy** (read-only top-down tree with read-only face-piles), **Manage** (the existing `team-management.tsx` embedded UNCHANGED — invite / position-edit / remove), and a **People** layer on the Editor/Hierarchy cards. **People view (people brief)**: each position card shows a face-pile (≤4 initials + "+N") + count + ▾ expand → member rows (initial + email + role badge + move-to-role select + remove ✕ + "+ assign member"), with a one-line hint "Changing a position updates this person's access."; members with `position_id IS NULL` show in an **Unassigned-members tray** (`unassigned-members-tray.tsx`). **No new mutation endpoints** — people moves/removes/assigns reuse `PATCH`/`DELETE /api/v1/team` (which re-derives role via `deriveRole`, honoring self-lockout / last-owner / owner-tier guards). **New API** `/api/v1/org-layers`: GET (enriched — each position carries a real `members[]` via `db.raw().auth.admin.listUsers()` email map, plus top-level `unassigned_members`; emails only from the tenant's own members), POST, `[id]` PATCH/DELETE, reorder. positions POST/PATCH accept `layer_id` (allowed for system positions; no role re-sync). All `scopedClient`, mutations admin-gated, reads gated on `canSeeNav("/team")`. New roles default to least-privilege `leadScope: "own"`. **Seeded** (mig `031`, applied to shared DB): Admizz got Leadership (Owner, Admin) + Team (Counselor, Viewer + 3 custom); the 2 it_agency tenants have no positions → no layers (correct). |
| `orca-ui` | `src/components/dashboard/orca/` (12 components) + `src/app/(main)/(dashboard)/orca/` (6 page shells) | **Universal, UI-ONLY** (shipped 2026-06-04, `43b074e` non-FF merge). Fills the existing `Ops \| Orca` sidebar tab switcher (was an "Orca coming soon" placeholder) with a 6-screen AI-orchestration shell **harvested verbatim** from the never-merged, 197-commits-stale `feature/ai-orchestrate-orca` branch (harvested, NOT rebased). **Static / mock data only** — no DB, no API, no agent logic; the real Orca is an external product that will power this surface later. 6 screens under `/orca/*` (Overview, Structure, Roles, Tasks, Agents, Compare). `shell.tsx` rewired so the tab is **route-coupled**: clicking Orca pushes `/orca`; any Ops route flips back to Ops mode; `navMode` derived from `pathname` (localStorage removed). **No gate** — every role / tenant / industry sees it. Harvested components import only `cn`, `lucide-react`, `next/link`, `react`, and local `./types` + siblings (zero shadcn dependency). Old branch's later phases (mig `009`, `/api/v1/orca/*` CRUD, agent wiring) **explicitly NOT ported**. |
| `positions` (RBAC) | `src/lib/api/permissions.ts` + `src/app/(main)/api/v1/positions/` + `src/components/dashboard/settings/positions-manager.tsx` | **Universal** configurable permission profiles (shipped 2026-06-04, Phases 1–4: `c71269b`/`a2a9534`/`eea97cc`). Tenant-scoped `positions` (mig `030`) hold a permissions JSONB: `nav` allow-list (hrefs), `pipelines` access, `leadScope` (own/all), `canEditLeads`, `dashboard.widgets`. Each has `base_tier` (owner/admin/member); `deriveRole(base_tier, leadScope)` keeps `tenant_users.role` synced (member+own→counselor, member+all→viewer; `canEditLeads`=branch-manager edit-all decoupled from role) so legacy `role` checks keep working. `position_id` (nullable) FK on `tenant_users`/`invite_tokens`; NULL=derive-from-role (unconfigured = byte-identical to pre-feature). **Single resolver** `resolvePermissions` + helpers (`canSeeNav`/`canAccessPipeline`/`canSeeWidget`/`shouldRestrictToSelf`/`leadQueryScope`) on `AuthContext.permissions` + `getCurrentUserTenant().permissions`. **3-layer enforcement**: API (lead-scope + pipeline filter + `canSeeNav` 403 on GET leads/pipelines/team/knowledge-bases), sidebar+page+dashboard (`getIndustrySidebarItems`/`shell` nav filter, `canSeeNav→redirect` on 14 pages, `canSeeWidget`), SSR helpers (`getLeads`/`getLead`/`getLeadsForPipeline` via `leadQueryScope` — closes SSR pipeline hole). CRUD `/api/v1/positions[/id]` (PATCH re-syncs all holders on base_tier/leadScope change; DELETE blocks system + non-empty); `PATCH /api/v1/team` `position_id` (owner-tier reject + self-lockout + last-owner guards). owner/admin hard-override to full access. Education tenants seeded 4 system positions (Owner/Admin/Counselor/Viewer, undeletable, identity-locked). Engine universal; only education seeded for now. |
| `settings` | `src/app/(main)/(dashboard)/settings/` + `src/components/dashboard/settings/` | Tenant settings, API keys, email rules, positions manager. |
| `dashboard` | `src/app/(main)/(dashboard)/dashboard/` + `src/components/dashboard/stats-cards.tsx` | Stats overview. |
| `knowledge-bases` | `src/app/(main)/(dashboard)/knowledge-bases/` + `src/app/(main)/api/v1/knowledge-bases/` + `src/components/dashboard/knowledge-base*.tsx` & `knowledge-bases.tsx` | **Universal** reusable knowledge library (shipped 2026-06-03, `db6bdc2`). Named KBs hold items of type `file`/`link`/`note`. NOT industry-gated (no manifest/registry registration; sidebar entry in `UNIVERSAL_NAV_TOP` after Dashboard). **Permissions**: all members read/download; owners/admins create/edit/delete (server `requireAdmin` on every mutation, UI hides controls). **Schema** (mig `029`): `knowledge_bases` + `knowledge_base_items` (own `tenant_id`), RLS members-read/admins-mutate. Items `status` col (`pending/processing/ready/failed`, v1 always `ready`) + emitted `knowledge_base_item.created` events are the future-ready hooks for the Orca embedding phase (pgvector NOT built). **Storage**: private bucket `knowledge-base-files` (path `${tenantId}/${kbId}/${itemId}.${ext}`, 25 MB app cap); signed upload via `POST [id]/upload-url` then client `uploadToSignedUrl`; signed download via `GET [id]/items/[itemId]/download`; app removes blobs on item/KB delete (fire-and-forget, non-fatal). **6 routes** under `/api/v1/knowledge-bases`: list+create, `[id]` CRUD, `[id]/items` (mixed-type add, idempotent file register on `item_id`), `[id]/items/[itemId]` PATCH/DELETE, `[id]/upload-url`, `[id]/items/[itemId]/download`. All `scopedClient(auth)`; file MIME/size re-validated server-side. |
| `notifications` | `src/app/(main)/api/v1/notifications/` + `src/lib/notifications.ts` + `src/components/dashboard/notifications-dropdown.tsx` | In-app notifications (table `notifications`, mig 015) + attention badges. Bell dropdown polls every 30s, deep-links via `notification.link` on click. **Triggers (`src/lib/notifications.ts`)**: `lead.assigned`/`lead.unassigned` (single + bulk), `invite.accepted`, `team.member_joined`, and (2026-06-01, `535c8ed`) `email.received` (education poll lib → inbox owner + lead assignee, 15-min collapse via `upsertThreadNotification`), `lead.created` (both create paths, `is_final`-gated, → assignee or `getTenantAdminRecipients`), `lead.stage_changed` (→ assignee + admins when `pipeline_stages.is_terminal`). Self-actions suppressed everywhere via `createNotificationsExcept(actorUserId, …)`. **Attention badges (2026-06-01)**: mig `028` adds `emails.read_at` (inbound backfill + partial index); `PATCH /api/v1/email/threads/[id]/read` (education-gated, counselor ownership check); `GET /api/v1/badge-counts` returns `{unread_notifications, unread_leads, unread_lead_ids}`; `useBadgeCounts` hook (`src/hooks/use-badge-counts.ts`, 30s poll); sidebar "All Leads" red badge in `shell.tsx`; Emails sub-tab unread badge in `activities-panel.tsx` with optimistic mark-read on thread expand. **Unread-lead model (2026-06-01, `3ff0ebe`)**: a lead is "unread" if the user has an unread notification linking to it (`link like '/leads/%'`). `unread_leads` = count of those distinct leads (= number of dotted rows); `unread_lead_ids` drives a **red dot next to the lead name in the All Leads table** (`leads-table.tsx`). Opening a lead clears its notifications via `POST /api/v1/notifications/read-by-link` (fired on `lead-detail-v2.tsx` mount), so dot + count self-heal on the next poll. **Activity-tab roll-up badge (2026-06-03, `5f4a96b`)**: `useEmailThreads` lifted from `ActivitiesPanel` to `lead-tabs.tsx` (always mounted) so the lead-detail **Activity top tab shows a red roll-up badge = sum of inner sub-tab notification counts** (today = unread inbound emails), red-takes-priority over the gray activities count; parent badge == Emails sub-tab badge (same threads state). `task.due` deferred (project-scoped tasks need a separate daily-cron brief). |
| `ai-chat` | `src/app/(main)/api/v1/ai/chat/` + `src/components/dashboard/ai-assistant/` | Placeholder; per-industry prompts come later. |
| `email-rules` | `src/components/dashboard/settings/email-rules-manager.tsx` + `/api/v1/settings/email-rules/` | Gmail auto-forward. |
| `form-autoresponder` | `src/lib/email/form-autoresponder.ts` + `render-template.ts` + config tab in `src/industries/_shared/features/form-builder/components/autoresponder-editor.tsx` | **Global send infra; config UI lives in form-builder (education_consultancy + construction).** Per-form confirmation/receipt email to the submitter with `{{merge-tags}}` echoing submitted fields. Config = JSONB `form_configs.autoresponder {enabled, fire_mode:'every'\|'first', subject, body_html}` (mig **042**). Shared merge engine `render-template.ts` resolves tokens from `custom_fields` → standard lead cols → `tenant_name`; body HTML-escapes VALUES then `\n→<br>`; subject not escaped. Sends via Resend `EMAIL_FROM`, logs every attempt to `automation_email_log` (mig **041**, RLS: members SELECT / service writes — also the shared log the parked Email Phase 1.2 will reuse). Fire-and-forget. Wired into BOTH submit entry points: `/api/v1/leads` (hosted form — new-insert, multi-step-finalize, dedup-fold) AND `/api/public/submit/[tenantSlug]/[formSlug]` (API-key integrations). `email-forward.ts` (the stage-rule lane) refactored onto the same render engine. **SHIPPED TO STAGE 2026-06-09** (`60bb891`, PR #11); awaiting prod GO. **Deferred (v2):** file attachments / generated download link (`{{catalog_link}}` currently just echoes the typed value); pipeline email-forward does NOT re-fire on dedup-fold re-downloads (product decision). |
| `lead-activities` | `/api/v1/leads/[id]/activities/` | Calls, emails, meetings. |
| `lead-insights` | `/api/v1/leads/[id]/insights/` | AI scoring (scaffolded). |

## Industry-aware

| ID | Location | Adapts by industry | Notes |
|---|---|---|---|
| `default-pipeline-stages` | DB seed in `industries.default_pipeline_stages` | Yes — each industry seeds different stages on new pipelines | Read by pipeline create flow. |
| `industry-entities` | `src/components/dashboard/settings/industry-entities-manager.tsx` | Yes — label changes per industry (Partner Colleges, Services, Project Types, etc.) | Only renders if `tenant.industry_id` is set. |

## Industry-scoped

| Registry ID | Location | Industries | Notes |
|---|---|---|---|
| `FEATURES.CHECK_IN` (`check-in`) | `src/industries/education-consultancy/features/check-in/` | `education_consultancy` | Student check-in with search + history + per-student detail. Gated at sidebar, page (`/check-in`, `/check-in/[id]`), and 4 API routes. |
| `FEATURES.FORM_BUILDER` (`form-builder`) | `src/industries/_shared/features/form-builder/` | `education_consultancy`, `construction` | **Now a `_shared` feature — promoted to `_shared` + enabled for `construction` 2026-06-07 (`f961970`, prod). First cross-industry shared feature** (folder `git mv`'d from `education-consultancy/features/`, history preserved; only the meta + manifests + 6 consumer import paths changed). Gate opens via BOTH manifests opting in (`construction/manifest.ts` + `education-consultancy/manifest.ts`) AND `formBuilderMeta.industries = [education_consultancy, construction]`. Templates are **industry-aware** via `getTemplatesForIndustry(industryId)` in `templates/index.ts`, threaded page→wizard→picker: education sees all 4 + Blank; construction sees **General Contact + Blank** only. First construction tenant: **Prime Ceramics** (`prime-ceramics`). Visual form builder + templates + public submit API. Gated at sidebar, 3 page routes (`/forms`, `/forms/new`, `/forms/[id]`), and 3 API routes. **UTM Link Builder added 2026-05-31 night (`6b9d741`)**: new `/forms/utm-builder` page + `<UtmLinkBuilder>` (form picker / paste-URL mode / utm_source-medium-campaign inputs) + `<UtmLinkList>` for managing saved links. Backed by `POST /api/v1/utm-links` (admin-only, `scopedClient`, validates URL syntax, verifies form_id tenant ownership before insert), `GET /api/v1/utm-links` (returns links with joined form name), `DELETE /api/v1/utm-links/[id]` (admin-only, defensive existing-check). All 3 endpoints industry-gated via `FEATURES.FORM_BUILDER`. Per-form `<AttributionEditor>` mounted in form-builder for setting default UTM values when the URL has no params. Schema: `form_configs.attribution` JSONB (mig 026) + `utm_links` table (mig 027, tenant_id FK CASCADE, form_id FK SET NULL, RLS select via get_user_tenant_ids + insert/delete gated by is_tenant_admin, intentionally NO UPDATE policy — records immutable, admins delete + recreate). **Per-form pipeline routing (mig 036, `feature/form-pipeline-routing`)**: optional `form_configs.target_pipeline_id` FK (ON DELETE SET NULL — deleted pipeline silently reverts to default). A new "Routing" tab in the form builder (`<PipelineRoutingEditor>`) lets admins pick from their tenant's pipelines via shadcn Select; submission lands at the target pipeline's first/default stage. Public submit resolver prefers the form target, falls back to the tenant default pipeline with `log.warn` if the target or its stage can't be resolved — never rejects a submission over a routing misconfiguration. PATCH handler validates that the chosen pipeline belongs to the auth tenant before saving. |
| `FEATURES.EMAIL` (`email`) | `src/industries/education-consultancy/features/email/` | `education_consultancy` | Email-from-CRM, 4-phase plan. **Phase 1 (2026-05-31 evening, `c9db7c2`)**: foundation only — no send yet. Migration 025 adds `user_id` + `display_name` to `connected_email_accounts` (RLS shifted from tenant-admin-scoped to user-scoped, with a tenant-admin SELECT policy for read-only oversight); backfill assigns existing rows to each tenant's owner. New tables: `email_threads` (gmail_thread_id + lead_id/contact_id link), `emails` (full RFC headers in `rfc_references` — Postgres keyword collision on "references"; both Gmail thread_id and RFC Message-ID stored for future Outlook swap with zero data migration), `email_sync_state` (per-account historyId checkpoint for Phase 3 polling worker). `googleapis` package installed. `gmail-client.ts` in feature folder wraps googleapis with 3 Phase 1 functions (`createOAuth2Client`, `getProfileEmail`, `refreshAccessTokenIfNeeded`); no send/list/get yet. Legacy `/api/v1/settings/email-accounts/gmail/callback` evolved to capture `user_id` from `authenticateRequest()` so the NOT NULL invariant holds for the legacy email-forward connect flow. 4 new endpoints under `/api/v1/email/inboxes/*` (GET list + POST connect + GET callback + DELETE /[id]), all industry-gated; callback gates BEFORE code exchange; HMAC-signed state validated against current session; OAuth uses `prompt=consent` (mandatory for multi-inbox refresh tokens). `<InboxConnector>` client component on `/settings` (conditionally rendered for education_consultancy only, wrapped in `<Suspense>` for Next.js 16's `useSearchParams` rule). No sidebar entry — compose is invoked from lead/contact detail in later phases. **Phase 2 (2026-05-31 evening, `977fc44`)**: compose + send + log on lead detail. 14 files / +1,642 / -38, no schema changes (Phase 1's mig 025 sufficient). `gmail-client.ts` gains `sendMessage()` using `googleapis.gmail.users.messages.send` + `nodemailer/lib/mail-composer` for RFC 822 construction; Message-ID set explicitly as `<uuid@edgex-crm.com>`; calls `refreshAccessTokenIfNeeded` first then reuses the refreshed access_token via `setCredentials` to skip a second OAuth roundtrip; returns `refreshed_credentials` so the caller can persist. New `POST /api/v1/email/send` (industry-gated, `scopedClient(auth)`, from-account ownership check `.eq("user_id", auth.userId)` returns 403 not 404 on mismatch, server-side merge field interpolation for `{{first_name}}`/`{{last_name}}` BEFORE Gmail call so stored content matches sent content, counselor-scoped lead lookup via `.eq("assigned_to", auth.userId)` for the merge interpolation, persists `email_threads` + `emails` rows + emits `email.sent` event, fire-and-forget persist of refreshed token). New `GET /api/v1/email/threads?lead_id=X` (industry-gated, `scopedClient`, counselor-scoped via `sender_user_id`, PostgREST embed `email_threads!inner(...)` with belt-and-suspenders `.eq("email_threads.tenant_id", ...)` on the join column). 4 new UI components in feature folder: `<ComposeEmailDialog>` (modal with From/To/Cc/Bcc/Subject/TipTap body; client-side Send disabled until all required fields non-empty; on Send success → toast + optimistic prepend + close modal), `<FromAccountPicker>` (3 distinct states: 0 inboxes → Link to `/settings#connected-inboxes` + Send disabled, 1 → disabled with the inbox shown, 2+ → enabled), `<TipTapEditor>` (controlled wrapper — `value` synced via `editor.commands.setContent(value, { emitUpdate: false })` with equality guard; TipTap v3 options-object API since v2 positional-boolean form doesn't compile in v3; StarterKit + Link extensions; toolbar Bold/Italic/Link/BulletList/OrderedList), `<SentEmailCard>` (expandable list row matching ActivityCard visual shape with ✉ Sent badge). 2 new hooks: `useConnectedInboxes` (shared by `<InboxConnector>` and `<FromAccountPicker>` so list stays consistent on disconnect/reconnect/send), `useSentEmails` (wraps GET /threads). `ActivitiesPanel` evolution: Emails sub-tab now renders combined list of new sent emails (✉ Sent badge) + legacy logged emails from `lead_activities` (📝 Logged badge), sorted desc by `sent_at`/`created_at`; new "Compose Email" primary CTA gated by `industryId === "education_consultancy"` (non-education tenants only see "Log past email"); compose modal dynamically imported so TipTap (~150KB) only loads when modal opens — `/leads/[id]` first paint unaffected. Decisions locked in: every send creates a NEW `email_thread` (no `threadId` param to Gmail API — Phase 3 adds reply continuation); `body_text: null` on insert (display side derives from html); from-account ownership returns 403 not 404 to never leak existence of another user's account. **Phase 3 (2026-05-31 night, `6acd9f8`)**: inbound polling + thread display + reply-from-CRM. 13 files / +1,215 / -238 (includes 2 orphan deletes — `use-sent-emails.ts` + `sent-email-card.tsx`, replaced by `use-email-threads.ts` + `email-thread-card.tsx`). Email feature is now bi-directional. New `/api/internal/email/poll` (NOT under `/api/v1/`; bearer-auth via `INTERNAL_CRON_SECRET` env var, fail-closed if unset; per-account Promise.allSettled chunks of 5 with try/catch isolation; DB-level industry gate via `tenants!inner(industry_id)` so non-education accounts aren't polled; History API 404 → bootstrap from `profile.historyId`; idempotency via `idx_emails_gmail_message` unique index; reply matching = Gmail `threadId` primary → RFC `In-Reply-To` fallback → `References` chain fallback; orphan inbound silently dropped). Hit by `.github/workflows/email-poll.yml` GH Actions cron every 5 min + `workflow_dispatch` for manual trigger. `POST /api/v1/email/send` extended with optional `reply_context: { thread_id, in_reply_to, references[] }` — validates same-account constraint (400 `REPLY_ACCOUNT_MISMATCH` on mismatch), reuses thread, increments `message_count` + `last_message_at`. `sendMessage()` wires Phase 2's previously-stubbed `threadId` + `inReplyTo` + `references` so Gmail threads outbound replies and recipients' email clients group via headers. `GET /api/v1/email/threads` rewritten to return embedded `emails(...)` shape via PostgREST; counselor scoping changed from broken `sender_user_id` (NULL on inbound — silently dropped every received message) to 2-query own-accounts approach via `.in("connected_email_account_id", ownAccountIds)`. New `gmail-client.ts` exports: `listHistory()` (wraps `gmail.users.history.list`, returns `messageAddedIds[]` + 404→`expired:true`), `getMessage()` (wraps `gmail.users.messages.get` format:full, delegates to `parseGmailMessage()`). New `gmail-parser.ts` helper parses Gmail message payload extracting headers + body parts; custom RFC822 address parser. New `<EmailThreadCard>` with collapsed/expanded UX + Reply button. `<ComposeEmailDialog>` extended with optional `replyContext` prop — locks From picker to thread's account, pre-fills To + "Re: ..." subject + empty body (no quoted block per design — quoting defers to Phase 4+). `<ActivitiesPanel>` Emails sub-tab: threads section + "Past activity" subheader for legacy `lead_activities` (threads are the active surface). Emits `email.received` events per inbound; `email.sent` payload gains `is_reply: boolean`. NEW env var `INTERNAL_CRON_SECRET` (per env; generate with `openssl rand -hex 32`; dev + prod separate values; also set as GH Actions secret `INTERNAL_CRON_SECRET_DEV` and `_PROD`). NO schema changes — Phase 1's mig 025 already had `direction='inbound'`, `received_at`, `in_reply_to`, `rfc_references[]`, `sender_user_id NULL`, and `email_sync_state.connected_email_account_id` as PRIMARY KEY (verified — onConflict upsert works). **Phase 4 (pending)**: contact-detail Email tab + Account 360 activity feed integration + subject search + unread badges + parent CC merge field for education + attachments + quoted-block on reply body. |
| `FEATURES.CRM_CONTACTS` (`crm-contacts`) | `src/industries/it-agency/features/crm-contacts/` | `it_agency` | CRM Contacts — people at B2B accounts. Full CRUD (Phase B): contacts list + detail + ContactForm dialog + ContactStatusBadge. 6 API routes (list, create, get, patch, soft-delete, by-account). Soft-delete clears `accounts.primary_contact_id`. Primary contact pill on AccountDetailPage. Project↔contact junction wiring (Phase C): 2 symmetric API routes (`contacts/[id]/projects` POST/PATCH/DELETE + `projects/[id]/contacts` GET/POST/PATCH/DELETE), Projects section on contact-detail, Contacts section on project-detail, `ProjectContactPicker` dialog. Primary-contact-per-project enforced at DB (partial unique index) + 409 PRIMARY_TAKEN in API. Lead → Contact conversion (Phase D): `POST /api/v1/leads/[id]/convert` with TOCTOU-safe atomic update (same pattern as time-entries approve/reject — `.is("converted_at", null)` precondition + orphan contact cleanup on race-loss + 409). `ConvertLeadDialog` on `lead-detail-v2.tsx` defaults to "use existing account" pre-selected when `lead.account_id` is set, "create new" otherwise. NO_ACCOUNT sentinel for the Radix Select. Counselor scoping enforced server-side; `contact.assigned_to` mirrors lead. Cross-cutting `converted_at IS NULL` filter added to all default leads-fetching surfaces (queries.ts `getLeads`/`getLeadsForPipeline`/pipeline counts, `/api/v1/leads` GET, `/api/v1/accounts/[id]/leads` GET, `/api/v1/pipelines` GET, `/api/v1/pipelines/[id]` GET per-stage counts, leads bulk verification reads) with optional `?include_converted=1` flag for future archive view. `getLead()` and `/api/v1/leads/[id]` GET intentionally NOT filtered — preserves read-only access to converted leads via "Converted to <contact>" link pill on the original lead. Phase E (2026-05-27): 20-step smoke matrix run end-to-end (13/13 admin API + 4/4 counselor + Admizz + 3 visual); zero bugs surfaced; CRM Contacts v1 closed. |
| `FEATURES.ACCOUNTS` (`accounts`) | `src/industries/it-agency/features/accounts/` | `it_agency` | B2B accounts (agencies/employers) with linked projects and leads. Gated at sidebar, 2 page routes (`/accounts`, `/accounts/[id]`), and **10 API routes** (accounts, projects, tasks + v2's billable-summary/team/activity). **2026-05-29 evening (v1)**: `/accounts/[id]` rewritten as a 3-column **workspace 360° page** (`d1a4b89`, squash from `feat/account-detail-360`). CRM-expert-informed PSA framing (not Contact-360 symmetric): right column work-status-first (HealthSnapshotCard with project-status dot row + conditional OpenLeadsCard; no Lead Provenance card — dropped because lead-to-account lacks the 1:1 mapping); Primary Contact promoted to left header (identity attribute); middle column = 5-tab strip (Overview · Projects · Contacts wired; Activity + Billing disabled "Coming soon"). 10 new subcomponents under `components/account-detail/`. Backend extends GET `/api/v1/accounts/[id]` with `owner_email` (raw().auth.admin.getUserById), `project_status_mix` (6-key count reduced in app code), `open_leads_count` (exact HEAD count) via Promise.all. Cross-cutting: `PROJECT_STATUS_MAP` exported from `time-tracking/status-badge` as single label source-of-truth. **2026-05-31 afternoon (v2)**: `/accounts/[id]` extended with billable totals + Account Team card + Activity tab content (`0ec69a1`, squash from `feat/account-detail-360-v2`). 18 files, +1,080 / -35. KEY INFO gains 3 billable rows: Billable this month (with ▲/▼/—/New delta pill vs last month — `formatBillableDelta` util), Billable hrs this month (`XX.X hrs`), Lifetime billable. Counselor sees "Your hours only" hint. `AccountTeamCard` slotted between HealthSnapshotCard and OpenLeadsCard — two-group structure (Owners pinned first + Contributors filtered to last-90-days active, sorted by hrs-this-month desc) with role pills (Account Manager purple-50/700, Project Lead blue-50/700, Contributor gray-100/600). Activity tab wired (was disabled in v1) — union of events table rows + derived time-logged stream aggregated by `user+day+project` (one row per group, not per entry — CRM-expert critical rule). Paginated 30/page with "Load more". 3 new API endpoints: `billable-summary` (3 parallel queries for this_month/last_month/lifetime, `time_entries JOIN projects` filtered to billable + approved), `team` (owners + contributors with parallel email lookups), `activity?page=1&limit=30` (events + time_entry events filtered by payload.account_id + derived time-logged merged + paginated). 5 `emitEvent()` additions on existing routes: account.updated + project.updated + time_entry.approved + time_entry.rejected + lead.converted. 2 new utils under `src/lib/`: `format-billable-delta` + `format-relative-time`. One fixback rolled into squash: time_entry events were silently dropped from the Activity feed (root cause: brief query design didn't cover time_entry entity_type); fix added a third parallel events query with `.contains("payload", { account_id: id })`. **V3 deferrals**: sparkline / trend chart on KEY INFO; Billing tab content (invoices + retainer + per-project breakdown); filter chips on Activity ("All / Time / Projects / Contacts / Changes"); per-user billable detail page; last_activity_at on list; at-risk health score; MRR/retainer model. |
| `FEATURES.PROJECT_BOARD` (`project-board`) | `src/industries/it-agency/features/project-board/` | `it_agency` | Unified Project Workspace at `/projects` — admin-only v1. Four views over the same dataset with lifted, URL-encoded filters: **Board** (kanban with drag-drop + TOCTOU `expected_status` precondition + card metrics), **Table** (sortable columns + inline owner/status editors), **Tasks** (cross-project shadcn Table with 8 sortable columns + inline edits for status/assignee/priority/due/tags + log-time-from-row pre-filling LogTimeDialog), **Members** (one section per team-member-with-work; non-N+1 aggregation ≤3 requests; expand/collapse persists). Migration 024 adds `tasks.assignee_id` + `due_date` + `priority` + `tags TEXT[]`, plus `projects.owner_id` + `accounts.owner_id` and 6 indexes. New `GET /api/v1/tasks` cross-project endpoint (FEATURES.PROJECT_BOARD gate, counselor-scoped `assignee_id = auth.userId`, paginated max 200, filters: project/account/assignee/status/priority/tags/due/q). New `GET /api/v1/tasks/tags` for tenant-wide tag pool. `PATCH /api/v1/tasks/[id]` extended (assignee_id + due_date + priority + tags, back-compat via `"key" in body` for nullable fields). Tag UX is a Notion-style `<TagMultiPicker>` (search + autofocus + checkable rows + Create-new fallback + case-insensitive duplicate guard); used in both per-task and per-filter surfaces; ANY-match filter semantics via `.overlaps()`. Phase 5 polish: keyboard shortcuts (`b`/`t`/`k`/`m` for views, `/` focus search, `Esc` clear), per-view empty states with Clear-filters CTA, comprehensive a11y attributes, single `router.replace({ scroll: false })` in `use-workspace-filters.ts` so deep links and keyboard nav never jump scroll. Counselor scoping enforced on /api/v1/tasks (own-assignee filter). Admizz tenant: sidebar item absent, `/projects` 404, API 403. |
| `FEATURES.TIME_TRACKING` (`time-tracking`) | `src/industries/it-agency/features/time-tracking/` | `it_agency` | Time entries + approvals + billable totals. Gated at sidebar, 3 page routes (`/time-tracking`, `/time-tracking/projects/[id]`, `/time-tracking/approvals`), and 6 API routes (time-entries + approve/reject + summary). Phase 5 (2026-05-27): rates + billable totals close v1. `lib/rates.ts` (`resolveEffectiveRate`: project default_rate → member default_hourly_rate → 0) + `lib/totals.ts` (`calculateBillableMinutes`/`calculateBillableAmount`, filtered to is_billable + approval_status='approved'; uses `rate_snapshot` so historical invoices stay immutable). Atomic `rate_snapshot` on approval: parallel fetch project + member rates → compute in app code → single atomic UPDATE with the existing TOCTOU `.eq("approval_status", "pending")` precondition preserved. Team route `/api/v1/team` PATCH plumbs `default_hourly_rate` (admin-only, validates ≥0 or null; not industry-gated because the column is on a universal table). Shared `RateInput` component (`$`-prefixed numeric) used by `ProjectForm` (in `accounts/`); inline rate editor on `/team` gated to it_agency via `showRates = industryId === "it_agency"`. `/api/v1/time-entries/summary?dimension=member|project|account&from=&to=` with counselor scoping (non-admins query-filtered to own user_id + group-level safety net for dim=member). Billable totals UI: project-detail card above Contacts section, approvals-queue projected `$X.XX @ $Y/hr` per pending row, "Billable $" tile on the home stats strip. |
| `FEATURES.ITINERARY` (`itinerary`) | `src/industries/travel-agency/features/itinerary/` | `travel_agency` | **NEW INDUSTRY — `travel_agency`, registered 2026-06-10.** Mig **043** seeds the `industries` row incl. `default_pipeline_stages` (New Inquiry→Qualifying→Itinerary Sent→Revising→Booked/Lost); registry `INDUSTRIES.TRAVEL_AGENCY` + `FEATURES.ITINERARY`/`TRIP_INQUIRY`; manifest + `_loader` registration; `Plane` sidebar icon. First tenant **Arya Travels** (`arya-travels`). **Itinerary/quote builder**: per-lead day-by-day itinerary + priced line items in `leads.custom_fields.itinerary` (no new table); grand total via `lib/travel/currency.ts` (`formatMoney`, NPR default + multi-currency map); printable branded **proposal** with tenant logo. Launched from the lead's **Itinerary** tab (`lead-tabs.tsx`, gated `industryId === "travel_agency"`); **Itineraries list** at `/itineraries` (feature-gated → `notFound()` for non-travel). **Trip Inquiry panel** = industry-aware branch in universal `lead/key-info-section.tsx` (destination/dates/pax/budget/trip-type/flexibility in `custom_fields`, no migration); trip-type enum `leads/trip-types.ts`. **Packages** = the `industry-entities` catalog (`entity_type` = Package), 8 seeded for Arya. **Package-of-interest** on leads via existing `lead.entity_id` (next build). Reserved-key filter `lib/leads/reserved-custom-fields.ts` keeps `trip_*`/`itinerary` out of generic custom-field lists. Non-travel tenants 404/hidden (verified). |

## Shared

| Registry ID | Location | Industries | Notes |
|---|---|---|---|
| `FEATURES.FORM_BUILDER` (`form-builder`) | `src/industries/_shared/features/form-builder/` | `education_consultancy`, `construction` | First cross-industry shared feature (promoted 2026-06-07, `f961970`). See the full row under **Industry-scoped** above for behavior, gating, UTM/attribution, and the industry-aware template detail. Add a third industry by registering `{ meta: formBuilderMeta }` + a sidebar item in that industry's `manifest.ts` AND adding its id to `formBuilderMeta.industries` — never copy the folder.

---

## Industries

| ID | Name | Tenant today | Manifest |
|---|---|---|---|
| `education_consultancy` | Education Consultancy | Admizz Education (slug: `admizz`) | `src/industries/education-consultancy/manifest.ts` |
| `it_agency` | IT Agency | Zunkiree Labs (slug: `zunkireelabs-crm`) | `src/industries/it-agency/manifest.ts` — Contacts, Accounts, Time Tracking, Project Workspace |
| `construction` | Construction | — | `src/industries/construction/manifest.ts` (empty stub) |
| `real_estate` | Real Estate | — | `src/industries/real-estate/manifest.ts` (empty stub) |
| `healthcare` | Healthcare | — | `src/industries/healthcare/manifest.ts` (empty stub) |
| `recruitment` | Recruitment | — | `src/industries/recruitment/manifest.ts` (empty stub) |
| `general` | General | — | `src/industries/general/manifest.ts` (empty stub) |

---

## Workflow reminders

- New feature for one industry only? → `src/industries/<id>/features/<feature>/` + add to `_registry.ts` + register in `<id>/manifest.ts`.
- Need an existing feature in a 2nd industry? → `git mv` to `_shared/`, opt-in via both manifests. **Never copy-paste.**
- Universal feature? → `src/app/(main)/(dashboard)/<feature>/` — no manifest changes.
