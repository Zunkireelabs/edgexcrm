# Status Board — Lead Gen CRM

> Live checklist of open user-side actions, decisions, and questions. Companion to [SESSION-LOG.md](./SESSION-LOG.md). Update as items resolve.

Last updated: 2026-09-07 (**Board triaged** — split into Live / Parked, 18 items archived, 13 engineering lessons relocated; see the note below.) Prior: 2026-09-04 (**Orca is LIVE ON PROD** — read-only, owner-only, for `zunkireelabs-crm` + `admizz`. All four D5 blockers closed. New items below on verifying Langfuse masking against real prod traffic and on the CI coverage gap.) Prior: 2026-08-23 (**Admizz tenant custom sending domain — LIVE ON PROD, `hello@admizz.com`.** New item below on the campaigns follow-up question.) Prior: 2026-08-15 (**SMS via Aakash — PR #390 needs Anish's approval; branded sender ID needs NTC/Ncell registration.** Two new top items below.) Prior: 2026-07-30 (**PROD-WEDGE — prod resilience hardening.** 2026-07-30 prod wedged 1h45m dark, nobody notified. Phase 1 repo changes [memory caps, autoheal, uptime watchdog] done on `feature/prod-resilience-limits`, PR to `stage` — see new top item below.) Prior: 2026-07-27 (**AI-NATIVE PHASE 5 (AGENT SPINE) — integration-passed on `feature/ai-phase5-agent-spine`, staged for a `stage` PR.** New top item below: env vars owed on stage (`AI_TOOL_APPROVAL_SECRET` overdue, `AI_AGENTS_ENABLED`, `AI_MCP_ENABLED`) + deferred live verifications. **Note: this doc has not been kept current since 2026-07-02** — most items below that date are historical; treat anything not re-touched recently as possibly stale and cross-check against SESSION-LOG before acting on it.) Prior: 2026-07-02 (**CHECK-IN COUNSELOR-ASSIGN ROUTING PROMOTED TO PRODUCTION** — `main` @ `2248482`; mig **100** `lead_notes.edited_at` applied to prod (953→953, additive/reversible, per-action GO). Walk-in check-ins route to **Prospects** only when a counselor is assigned, else **Qualified** (lead-exec self-owned / branch-manager shared pool); counselor-gated auto-promotion via `resolvePositionSlug()`; checker retains view access via `lead_collaborators`; counselor-dropdown visibility fix. Verified logic-level (code review + build + stage slug check) + Sadin confirmed in prod UI; full S1–S6 UI run NOT completed (Sonnet browser automation abandoned). **Next free migration number = 101** (100 now on prod; mig 101 `get_scoped_leads` on branch `fix/lead-scoped-visibility`). Prior: **fix/fall-back → stage PR #94 merged, deployed to stage, verified live on dev.** Branch-manager nav gap (mig 099) applied to **STAGE only** — prod promotion pending Sadin GO. PR #93 docs/prod-db-change-policy merged to stage this session. Prior: Dev inbound email now **auto-polls every 2 min via a VPS root crontab** — replaced manual-curl-only; GH Actions cron never ran on dev because schedules register only from the default branch. Verified: cron fires, `last_synced_at` advances, 0 errors. See [reference memory] + the dev-auto-poll item below. Notifications + badges + unread-lead red dot all shipped to stage + deployed green. Email Phase 3 dev smoke ✅ COMPLETE — full bi-directional loop verified on dev; `INTERNAL_CRON_SECRET` set on dev + GH secret `INTERNAL_CRON_SECRET_DEV`. Notifications + attention-badges ✅ shipped to stage (`535c8ed`) — needs dev smoke (3 live-path items). Email Phase 2 ✅ + UTM tracking ✅ + Email Phase 3 ✅ all shipped to stage. Phase 3 closes the email-feature loop: counselor sends → recipient replies → ~5 min later inbound appears in CRM thread → counselor replies from CRM → recipient sees reply continue same Gmail thread. **NEW env var Phase 3 introduces — set BEFORE prod promotion**: `INTERNAL_CRON_SECRET` per env + `INTERNAL_CRON_SECRET_DEV` / `_PROD` as GH Actions secrets. Side-quest UTM merge (`6b9d741`) was Anish's `utm` branch cherry-picked onto current stage by Opus because his branch was 9 commits behind + had a 025 migration collision. Migrations 026 + 027 still need manual apply to dev DB before UTM features work on dev. Phase 1+2 prod promotion pending env-var setup + Sadin's go-ahead. Account 360 v2 live on prod at `0f58a0a`. **Focus: `education_consultancy`** — Email Phases 1✅, 2✅, 3 + 4 ahead. UTM live.)

---

> **Triaged 2026-09-07.** This board had grown to 58 undifferentiated open items, some dating to May, under a single heading. They are now split by whether they actually compete for attention. Nothing was deleted: 18 resolved/overtaken items moved to [`docs/archive/STATUS-BOARD-ARCHIVE-2026-09-07.md`](./archive/STATUS-BOARD-ARCHIVE-2026-09-07.md) with their full text, and 13 entries that were engineering *lessons* rather than tasks moved to [`docs/reference/06-ENGINEERING-LESSONS.md`](./reference/06-ENGINEERING-LESSONS.md). **Keep it this way:** an item that is done gets moved out, not left ticked in place.

---

## 🔴 Live — needs Sadin decision / action

*Ten items. If this section grows past ~12, something needs to move to Parked.*

- [ ] **AI — verify Langfuse masking against the FIRST REAL PROD TRACES. Highest-value open item; do it while the traces are fresh.** Orca went live on prod 2026-09-04, so real Admizz student PII now reaches OpenAI on every Ask Orca call, and Langfuse (EU region, 30-day delete) traces it. The masking added in #473 (`mask` passed to the Langfuse client at `telemetry.ts:141`, `maskComment()` for scores) has **never been observed against production traffic** — only unit-tested. Open the newest `assistant.chat` trace at cloud.langfuse.com and confirm: **input and output read `[masked]`** (the design is allow-list, fail-closed — strings pass only if their key is in `SAFE_STRING_KEYS`, id-shaped keys only if the value is a real UUID), while `tenantId`/`userId`/`model`/`environment` stay readable and all numbers pass through. **If any real prompt, reply, or student name is visible, that is a disclosure problem, not a bug to sit on.** Also confirm the trace exists at all (proves the prod Langfuse keys are wired) and that `environment` distinguishes prod from stage — D5 records that ONE Langfuse project holds both key pairs.

- [ ] **SMS — register a branded sender ID with NTC/Ncell. Sadin's action, 5–10 business days, start it in parallel now.** Every Aakash message currently goes out under the account-level sender ID **`AT_Alert`**, which reads as spam to a student. Sender ID is account-level, not an API parameter, so this cannot be fixed in code — and because there's no inbound channel, every message must also self-identify in its body (`"Admizz: "`). **No code depends on this; the first real blast does.** Registration is 3–11 alphanumeric characters. While you're in writing with Aakash, the plan's open questions are still unanswered and worth asking in the same thread: their rate limits, max recipients per call, the cost of renting a keyword/shortcode (which would give us real STOP handling instead of a link), and their stated compliance requirements — no primary NTA directive was findable online, so the commonly-quoted "8am–8pm, STOP/BAND" rules are being shipped as a **product default, not a claim about the law**. Related standing risk: 17,334 real student phone numbers are in scope, so **grant Admizz 2,000 credits to start, not 25,000** — the balance is the last backstop against an accidental mass blast.

- [ ] **`.env.local` has no backup — added 2026-07-30 (PROD-HARDENING BRIEF §4.2).** `RESEND_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `INBOX_TOKEN_ENC_KEY`, `NEXTAUTH_SECRET`, and others exist **only** on the boxes themselves — not in git, not in GitHub. `INBOX_TOKEN_ENC_KEY` is the sharp one: lose it and every stored OAuth token becomes permanently undecryptable. **This is the only open item where a mistake is irreversible** — the other two below are recoverable. Copy both files (prod `/home/zunkireelabs/devprojects/lead-gen-crm/.env.local`, stage `.../lead-gen-crm-dev/.env.local`) into a password manager. Owner: Sadin.

- [ ] **CI never runs 22 integration tests — silent coverage hole in the money/compliance paths.** `.github/workflows/ci.yml:117` runs `npm run test` with **no Supabase service or start step**, so six suites self-skip on every CI run: `email/outbound/send` (10), `sms/suppression` (3), `email/outbound/suppression` (3), `sms/optout` (2), `email/outbound/unsubscribe` (2), `sms/credits-idempotency` (2). They cover **SMS credit idempotency, suppression lists and opt-out**. Verified 2026-09-04 with local Supabase up: **all 30 tests in those files pass** — the coverage is healthy, CI simply never asks for it. The danger is that they skip *silently*, so the suite reports green either way. Fix is its own small PR: add a Postgres/Supabase service to the Test job, or make those suites fail loudly instead of skipping when the DB is absent. (Sibling hole **closed** in #492: `.test.tsx` files were never matched by vitest's `include`, so the `/orca` route-gate test never ran.)

- [ ] **Tell Admizz that Orca is live.** They gave written consent on 2026-09-03; closing that loop is owed. Two things they need to know up front: it is **owner-only** for now (of their 26 members only Sadin and Manish K Sah can see it — per-user AI access levels are designed but unbuilt), and the **"AI & Orca" settings tab is a `Coming soon` placeholder**, not a broken page.

- [ ] **Remove `OLD_SUPABASE_*` from prod `.env.local` + rotate that old service-role key — added 2026-07-30 (PROD-HARDENING BRIEF §2).** Prod's environment still carries `OLD_SUPABASE_URL` + `OLD_SUPABASE_SERVICE_ROLE_KEY`, left over from the 2026-06-21 database split. A stale **service-role** key (RLS-bypassing) sitting in a live environment is pure liability with no upside. **Verified safe to remove:** the only reader is `scripts/migrate-rku-leads.ts`, a one-time ETL that takes them as its *own* inputs and points at a different Supabase project (`ldsgsdjixzsljgkcktqu`) — nothing in `src/` reads them. Commands (Opus is blocked from editing prod `.env.local`, so this is Sadin's):
  ```bash
  ssh vps 'cd /home/zunkireelabs/devprojects/lead-gen-crm
    cp .env.local .env.local.bak-$(date +%F-%H%M)
    grep -n "^OLD_SUPABASE" .env.local            # confirm exactly the 2 lines first
    sed -i "/^OLD_SUPABASE_URL=/d;/^OLD_SUPABASE_SERVICE_ROLE_KEY=/d" .env.local
    docker compose -f docker-compose.prod.yml up -d --force-recreate app'
  ```
  ⚠️ `-f docker-compose.prod.yml` is mandatory — a bare `docker compose` in that dir hits the stray dev file and clobbers prod. Then **rotate/revoke the old service-role key** in the old Supabase project (browser task) — it has sat in an env file of unknown exposure history since June.

- [ ] **Delete the stray dev `docker-compose.yml` on the prod box** (`/home/zunkireelabs/devprojects/lead-gen-crm/docker-compose.yml` is a DEV config — container `leads-crm-dev`, dev domain — sitting next to the correct `docker-compose.prod.yml`). Bare `docker compose ...` in that dir defaults to it and **clobbers prod** (caused a ~3-min prod outage on 2026-06-08 when reloading an env var; restored via `docker compose -f docker-compose.prod.yml up -d`). Until removed/renamed, NEVER run bare `docker compose` there — use CI/CD or `-f docker-compose.prod.yml`. Also remove the stray `scripts/undo-one-merge.ts` (throwaway swept into a commit). See the prod-compose-landmine memory. **This is now Phase 2.2 of the PROD-WEDGE item above (rename, don't delete — reversible) — do it there, gated the same way, instead of as a standalone action.**

- [ ] **ADVANCED FILTERS — handed to Anish 2026-08-14. One blocking gate before the prod flag flip.** Phases 0–3.5 are merged to `stage` **and** promoted to `main` (#367/#368/#369/#371/#372); `origin/main` and `origin/stage` are identical on `src/lib/filters` + `src/components/filters`. **Prod is running the code with the feature OFF** — `deploy-staging.yml:64` passes `NEXT_PUBLIC_ADVANCED_FILTERS=1`, `deploy.yml` passes nothing, so prod renders the legacy toolbar. **Blocking:** PR #372 was merged with the body still reading *"Draft — verification in progress"* — the Phase 3.5 gate (Assigned-To facet counts on stage's 16.7k-lead Admizz tenant checked against `SELECT count(*)`) was never produced. A facet count ≤25 would mean the facet never moved server-side and Phase 2 isn't doing what it claims. **That verification comes before any new code and before the one-line prod flag flip.** Next phases briefed: 2b (collapse the remaining predicate mirrors — the PostgREST filter injection this phase used to carry was **fixed separately on 2026-09-02 by #476/#479**, so 2b is now a deduplication win, not a security fix) and 4 (Kanban adopts the shared state, ~350 duplicated lines deleted). Handover: `docs/ADVANCED-FILTERS-HANDOVER-BRIEF.md`; design + risk register: `docs/ADVANCED-FILTERS-PLAN.md`.

- [ ] **Backlog triage — nine PRs need a decision, none of them mine to make.** Sadin's four are ~2 months idle and likely superseded: **#26** (fetch ceiling), **#33** (leads-organise pagination — superseded by #332), **#91** (move/assign undo), **#98** (visibility predicate → RPC — the RPC shipped separately). Also **#310** (real unshipped email code: Reply-To display name) and **#285** (stale roadmap refresh). Hardik's **#176/#177/#396/#417** (#417 is DIRTY) and Anish's **#134** need a "still wanted?" rather than a close. Also: Manjila's `feature/dashboard-ui-refresh` is superseded by #493 and can be deleted once she confirms.

- [ ] **Admizz "email campaigns from admizz.com" — need to know what they actually want.** Sending is solved: Admizz's automation (autoresponders, pipeline email-forward, consent) now sends as `hello@admizz.com` (2026-08-23, `domain_verified` flipped on stage+prod). But their DNS shows a pre-existing Brevo DMARC `rua` (`rua=...,mailto:rua@dmarc.brevo.com`) — strong signal they already run bulk campaigns through Brevo. EdgeX's own bulk-send surface, Outreach, is still **log-only** (`markDraftSent` records a send without actually sending), so "campaigns from admizz.com" is not reachable in EdgeX today regardless of the domain work. Ask Admizz: keep campaigns in Brevo, or move them into EdgeX Outreach? The answer decides whether "Outreach actually sends" becomes a real, scoped phase or stays parked. Brief not yet written — scope it once the answer comes back.

---

## 🟡 Parked — real, but not now

*Genuine work with no current urgency. Promote to Live when it matters; don't let it crowd the section above.*

- [ ] **Migrate the remaining legacy authenticated routes to `scopedClient(auth)`**. **Re-scoped 2026-09-07: the real count is 67, not the ~33 this item claimed.** Measured on `origin/stage`: 335 route files total, 227 already on `scopedClient`, 92 still referencing `createServiceClient`, of which **67 also call `authenticateRequest()`** — those 67 are the actual migration target (the other 25 are public/internal routes where the service client is correct). Today they use raw `createServiceClient()` + manual `.eq("tenant_id", auth.tenantId)`. Four routes migrated as proof (`/api/v1/team` GET + DELETE, `/api/v1/notifications` GET). The wrapper handles tenant filter auto-injection + strips caller-supplied `tenant_id` from update/insert payloads; new routes must default to it. Migrate at the rate of "every route I'm already editing for another reason" — no big-bang refactor.

- [ ] **AI-native Phase 5 (agent spine) — env vars + deferred live verifications, now slated for stage.** Branch `feature/ai-phase5-agent-spine` is feature-complete + rebased onto `origin/stage` + integration-passed (2026-07-27; migs 185–190; all 4 gates green) — see `agent-spine`/`mcp-server` rows in FEATURE-CATALOG. Before/at the stage PR: (1) **`AI_TOOL_APPROVAL_SECRET` must be set in the stage GH environment — this is overdue, not pre-emptive**, since `AI_WRITE_TOOLS_ENABLED` is already ON on stage (the write-tool automation matrix + approval gate have been live-armed with no approval secret configured). (2) `AI_AGENTS_ENABLED` needs setting on stage for the background agents (Lead Triage / Daily Digest / Follow-up Drafter) to actually run. (3) `AI_MCP_ENABLED` needs setting on stage if the MCP server (`/api/mcp`) is to be exercised there. **Deferred live verifications, now slated for stage** (not provable pre-merge): Langfuse trace/score capture for agent runs, the Inngest-triggered agent cron end-to-end (event fire → run → output/approval), and browser click-throughs of Fleet / Review Queue / automation matrix / undo.

- [ ] **Lead Detail — Associations Rail (education_consultancy) — BUILT, STOP-AT-REVIEW. Awaiting Opus review + Sadin smoke.** Branch `feature/lead-associations-rail`. `npm run build` ✅ · `npx eslint --max-warnings 50` ✅ (0 errors, 31 warnings all pre-existing). **No push, no PR, no DB writes.** Brief: `docs/LEAD-ASSOCIATIONS-RAIL-BRIEF.md` (§4 plan A1–A6). **What shipped:** right rail for education prospects → compact `ApplicationsCard` (count + rows → `/applications/[id]` + Add button via extracted `AddApplicationToLeadSheet`); center Applications tab removed from `lead-tabs.tsx`; orphaned `applications-panel.tsx` deleted. **Gate matrix:** education + prospect → Applications card (no Checklist, no Applications tab); education + non-prospect → Checklist; non-education → Checklist. Next: Opus reviews + re-runs gates + pushes to stage; Sadin pixel smoke.

- [ ] **Unified Inbox — Phases 1+2+3a on STAGE; real WhatsApp LIVE on dev; NOT on prod.** `stage` @ `0279241`. Full detail + dev wiring + prod checklist: `docs/UNIFIED-INBOX-BRIEF.md`. Real WhatsApp proven both directions on `dev-lead-crm` (inbound + outbound + read receipts). Dev is fully wired (Meta app published, permanent System User token, WABA subscribed, 4 env vars, `*/1` auto-drain cron). **Open items:** (1) **Phase 3b** — near-instant inbound (inline-process after fast-ack; brief `docs/UNIFIED-INBOX-PHASE-3B-BRIEF.md`); inbound lags up to ~60s on dev today (1-min cron). Sonnet brief ready. (2) **Prod promotion** (separate GO): merge stage→main (code-only, mig 044 already on shared DB) + set 4 WhatsApp env vars on prod + **add a prod-URL inbox drain** (the `inbox-process.yml` workflow targets the dev URL) + connect channel on prod + update Meta webhook to prod callback. (3) **Production WhatsApp prerequisites:** real **privacy policy page** (Opus can draft) + **real business number** + **Business Verification** (test number = 5 recipients only). (4) **Phase 4** Messenger/Instagram (adapters are stubs); **Phase 5** AI agent runtime.

- [ ] **Domain migration `lead-crm` → `edgex.zunkireelabs.com` — PHASE 0 + PHASE 1 DONE (edgex LIVE on prod, dual-host); Phase 2 investigation DONE + PAUSED (call sign `EDGEX-DRAIN` — say "resume EDGEX-DRAIN"); Phase 3 pending.** Checkpoint at top of `docs/DOMAIN-MIGRATION-BRIEF.md`. **Phase 2 finding:** two ingestion channels — hosted forms (`/api/v1/leads`) are **308-safe & don't gate Phase 3**; only API-integration callers (`/api/public/submit`, `events.source='public_api'`) must be re-pointed/verified first. Dead key (`Orca-connection`) confirmed safe. Next on resume: read-only Sonnet follow-up for the true per-form api-channel list + caller fingerprints. Full runbook: `docs/DOMAIN-MIGRATION-BRIEF.md`; ship detail in SESSION-LOG `2026-06-15`. `main` @ `cdc84f9`; edgex serves with a valid LE cert (SAN covers both hosts), lead-crm unaffected. **Done:** Phase 0 pre-flight (DNS, Google OAuth `edgeX CRM` client +2 edgex URIs, Supabase redirect allow-list `edgex/**`) all screenshot-verified; Phase 1 code (dual-host Traefik + `NEXT_PUBLIC_APP_URL`→edgex + email-host constant [stays lead-crm] + CI health-check retry-fix). **NEXT — Phase 2 (drain):** silently re-point Admizz + Prime form embeds/links to the edgex URL (we control placement); confirm the dormant Admizz integration key (last used 2026-03-06) is dead; watch lead-crm traffic fall to ~0. **Phase 3:** swap lead-crm Traefik router → 308 redirect to edgex, then remove once traffic = 0. **Smaller open decisions:** set Supabase Site URL to edgex (currently `localhost`, pre-existing/low-impact); move email FROM-address to `@edgex` (needs Resend domain verification — separate task); delete the leftover disabled secret on the `edgeX CRM` OAuth client (the ⚠️ in Google console, after confirming the app uses the enabled one).

- [ ] **IT-agency surface pass — PAUSED, RESUMABLE.** Resume when education_consultancy work feels done (or whenever Sadin pivots back). Done so far in this pass: `/contacts` list + `/contacts/[id]` 360° detail, `/accounts` list + `/accounts/[id]` 360° detail (v1 + v2), `/projects` Board view, sidebar nav grouping. **Remaining when we resume**: `/dashboard` · `/leads` list (already mostly the reference) · `/team` · `/time-tracking` · `/time-tracking/approvals` · `/settings` · `/projects` Tasks/Members/Table views (round out). Same workflow per surface: Opus audits → writes brief ending with the Sonnet handoff prompt → Sadin pastes the prompt → Sonnet implements → Opus reviews + stage merge → Sadin smokes. **Possible `/projects` follow-ups** when resumed: Sort popover, Export CSV. (`+ New Project` button shipped 2026-09-05 on `feature/it-agency-delivery-self-serve`.) **Possible `/contacts/[id]` v2 follow-ups**: `contact_notes` table + timeline composer, `contact_activities` audit log, Log Meeting persistence, last interaction date computed field. **Possible `/accounts/[id]` v3 follow-ups (deferred from v2)**: sparkline / trend chart of billable $ over last 6 months on KEY INFO; Billing tab content (invoices + retainer + per-project breakdown — needs invoices model); filter chips on Activity tab ("All / Time / Projects / Contacts / Changes"); per-user billable detail page (drill-in from Account Team row); `last_activity_at` computed field on /accounts list; at-risk health score (needs defined signals — no activity 14d, missed deadlines, etc.); MRR/retainer model. **Possible sidebar follow-ups**: localStorage persistence of collapse state, retrofit aria-expanded on Public Forms toggle.

- [ ] **PROD-WEDGE — prod resilience hardening, remaining box ops.** 2026-07-30 prod wedged 04:28–06:13 UTC (1h45m dark; `restart: unless-stopped` never acts on health status, only on process exit) — the 404 users saw was Traefik dropping its router during the eventual restart, not an app bug. Full incident + fix design: `docs/PROD-RESILIENCE-BRIEF.md`; ops note `docs/reference/04-PROD-RESILIENCE.md`. **Done as of 2026-07-30:** memory caps live prod+stage; `autoheal` container deployed + labels wired, **verified ~2m30s recovery**; `NEXTAUTH_SECRET` manually added to `.env.local` on **both** prod + stage, containers recreated, verified present; detection closed via UptimeRobot (see item above). **Also done 2026-07-30 (was owed, now closed):** `NEXTAUTH_SECRET` GitHub-secret backfill — added to the `production` + `stage` GitHub environments (distinct values), promoted via #323/#324, and **verified effective on both boxes**: the managed `# --- BEGIN AUTH env ---` block is written and the GitHub-managed assignment is the one the container actually uses (proven by SHA-256 comparison against both `.env.local` lines, since dotenv "last wins" was the assumption the whole rotation depended on). The hand-added line remains underneath as a deliberate floor value — **do NOT remove it**; if the GitHub secret ever disappears, the unconditional `sed` delete would otherwise leave no secret at all and 503 Gmail inbox connect. Autoheal digest pin also confirmed: `sha256:d98a2df2…` matches the image actually running on the VPS (the one that passed the ~2m30s recovery test). **Still owed:** (1) rename the stray dev `docker-compose.yml` in the prod dir out of the way; (2) memory sampler cron; (3) the standing capacity decision (58 containers / ~26 GB declared limits on 11 GB RAM).

- [ ] **`deriveHomeTip` still snaps day boundaries in the server's timezone.** The last timezone-naive **server-side** site after #493 fixed the insights date filter. `home/page.tsx:54` is a server component and `tips.ts` uses `toLocalDateString(new Date())` to decide which tasks are overdue, so on the UTC container it is 5h45m off for every tenant (`tenants.timezone` defaults to `Asia/Kathmandu`). Low impact — it only decides which *tip text* shows — but it is the same bug class. Every other `setHours(0,…)` site was swept and is client-side, where browser-local is correct. Fix with `dayBoundsInTz` and a test in a timezone that is not the dev machine's.

- [ ] **Wire `events` table → webhook dispatcher**. Mutations emit to `events`; the dispatcher exists but isn't consuming. Until then, webhook delivery is broken.

- [ ] **Build per-industry AI agent prompts/tools**. The `industries/<id>/ai/agent.ts` slots exist and the manifest type reserves an `ai` field, but no real prompts/tools are wired. Education-consultancy first when prioritized.

- [ ] **Phase 4 + 4.5 Time Tracking smoke gaps** (carryover): shipped on visual-confirmation. Not-yet-verified: bulk approve/reject, non-admin member view, Admizz 404 on /time-tracking, CSV export contents, TOCTOU race two-window test. No production regression evidence — pure thoroughness backlog.

- [ ] **Promote tags UI to `_shared/` when a 2nd industry wants tags**. Today Student/Parent labels are hardcoded in education's check-in UI. When IT or another industry wants tags, move the UI to `src/industries/_shared/features/lead-tags/` and let each manifest define its own label set (Student/Parent vs Hot/Warm/Cold vs Buyer/Seller etc.) via per-industry config. Not blocking.

- [ ] **New `DELETE /api/v1/email/inboxes/[id]` should check `email_forward_rules.email_account_id` FK references before deleting.** The legacy `DELETE /api/v1/settings/email-accounts/[id]` returns 409 if rules use the account; the new endpoint does not. Theoretical for Phase 1 (no email-forward rules will be created against per-user inboxes); the FK is `ON DELETE SET NULL` so the worst case is a rule's `email_account_id` silently becomes NULL on disconnect. Add the equivalent check in Phase 2 or 3 — mirror lines 37-46 of `src/app/(main)/api/v1/settings/email-accounts/[id]/route.ts`.

- [ ] **(Optional second pass) Serialize stage + main deploys via a shared concurrency group.** The SSH `command_timeout` fix (`5ce03d2`) gives both pipelines 30m headroom, so dual-deploy contention is no longer fatal — but the contention itself still happens (both builds running on the same SSH host slow each other down). If we want to eliminate the slowdown entirely, unify `concurrency.group` across `deploy-staging.yml` and `deploy.yml` (e.g., `group: deploy-ssh-host`). Trade-off: a stage push during an in-flight prod deploy would explicitly queue and wait. Wait-and-see — if no further contention timeouts after the 30m bump, this can stay parked.

- [ ] **Address remaining low-severity code-review findings**: typed-as-`keyof typeof INDUSTRY_ICONS` for icon names (catch typos at compile time), runtime warning when scopedClient.insert silently overrides caller tenant_id, telemetry for industries-not-in-registry. None are bugs today; all worth doing on the next sweep.

- [ ] **Decide CLAUDE.md "Developer Persona & System Guardrails" fate** (parked from utm merge). Anish added +41 lines of meta-rules near the top of CLAUDE.md in commit `5679eaa`: Plan Mode default, sub-agents liberally, "demand elegance", autonomous bug fixing ("do not prompt the user for direction unless..."), `lessons.md` pattern, rigorous verification. Some pieces (rigorous verification) are useful; some (autonomous-fix-no-prompting) conflict with our prod-merge-requires-explicit-approval discipline; the `lessons.md` pattern overlaps with what SESSION-LOG + STATUS-BOARD already do. Three options: keep all, drop all, or cherry-pick the bits that fit. Whatever you decide, bring it back as its own PR — not bundled with feature work. Also Anish's feature-table additions had one mislabel (`Contacts (prospects)` shown as education_consultancy; actually `FEATURES.CRM_CONTACTS` is it_agency) — fix if we revisit.

- [ ] `docs/archive/research/ai-insight-*` (2026-03-28) — `013_lead_insights` migration + `/api/v1/ai/chat` stub suggest partial implementation. Is the AI insight feature still on the roadmap, or are the stubs dormant? The new `industries/<id>/ai/agent.ts` slots are reserved for per-industry prompts — when this work gets prioritized, that's the home.

---

## ✅ Recently closed

*Kept briefly for context, then swept to the archive.*

- [x] ~~**SMS Phase 1 — PR #390 needs ONE approval from Anish.**~~ ✅ **CLOSED — #390 merged 2026-08-15** (verified via `mergedAt`); Phase 2 (#391) followed. This item sat here for three weeks after it was already done — a reminder to check `mergedAt`, not the board. Original text follows for context:

- [x] ~~**SMS Phase 1 — PR #390 needs ONE approval from Anish. This is the only thing blocking the whole SMS track.** Branch `feature/sms-phase1-core` @ `6c05ff6b` → `stage`. All CI green, `mergeable: MERGEABLE`, `mergeStateStatus: BLOCKED`, **zero reviews**. `stage` requires 1 approval with admin-bypass off, so **Sadin cannot self-approve** — ask `ani-shh`. Reviewed twice by Opus with three HIGH bugs found and fixed (two non-idempotent credit RPCs; one provider-result attribution bug), each verified independently in psql / by reproducing the red test, not accepted on self-report. **Merging is low-risk:** the feature is entirely dark behind `SMS_ENABLED=false`, has no page/route/sidebar/Inngest surface, and migrations 202/203 are local-only (stage + prod ledgers both verified at 201). On merge they ride the normal stage auto-migrate job. **Downstream:** Phase 2 (`docs/SMS-PHASE2-BRIEF.md`) is briefed and correctly refusing to start until this lands; Sonnet stopped at that gate rather than running past it. Reviewer's entry points: `docs/SMS-PHASE1-BRIEF.md` (what it is, §2 = authoritative provider facts) and `docs/SMS-PHASE1-REVIEW.md` (what was wrong and how it was proven fixed).

- [x] ~~**PROD-WEDGE — detection: WATCHDOG_ALERT_TO outstanding, no live monitor.**~~ ✅ **CLOSED via UptimeRobot, 2026-07-30.** UptimeRobot keyword monitor (`https://edgex.zunkireelabs.com/login`, keyword `EdgeX`, 5-min interval) is the live detection layer, alerting sadin@/anish@/hardik@zunkireelabs.com — delivery tested end-to-end and confirmed by all three. `WATCHDOG_ALERT_TO` is no longer an open action (supplied 2026-07-30). `scripts/uptime-watchdog.sh` (Phase 2.4) is re-scoped from a blocker to an **optional second layer** — available, not installed anywhere, install path in `docs/PROD-RESILIENCE-BRIEF.md` §2.4 if ever wanted. See `docs/reference/04-PROD-RESILIENCE.md` (corrected 2026-07-30 — previously claimed the watchdog was installed on the dev box; it never was).

- [x] ~~**fix/fall-back (PR #94) — merged to stage 2026-07-01.**~~ ✅ 6 items shipped: branch-manager nav gap (mig 099); list-funnel pipeline board (`ListFunnelBoard`); check-in auto-promote to Prospects; check-in history card; branch-scoped unassigned lead visibility (`requireLeadBranchAccess` + `getLeads`/`getLead`/`getLeadsForPipeline`); back-nav fix. Verified live on dev-lead-crm.zunkireelabs.com as Admizz branch-manager Bijay Dahal. Briefs archived: `BRANCH-MANAGER-LEAD-SCOPE-BRIEF.md` + `BRANCH-SCOPE-IN-CHUNK-CLEANUP-BRIEF.md`.

- [x] ~~**Campaigns (prediction leaderboard) — Phase 1+1.5+1.6 + Phase 2 ALL SHIPPED TO PROD ✅**~~ **FEATURE COMPLETE (call sign `CAMPAIGN-KICKOFF`).** Brief: `docs/archive/features/CAMPAIGNS-BRIEF.md` (safe to archive). Industry-scoped (`education_consultancy`/Admizz) Campaigns nav + FIFA WC 2026 "Predict & Win" leaderboard on `worldcup-predict-win`. **Live on edgex:** internal admin leaderboard (ESPN auto-fetch/store/auto-lock, gear+Refresh) + **public masked API** `GET /api/public/campaigns/<token>/leaderboard` (no PII, CORS, rate-limited) + gear "Agent prompt" (task-loaded LLM handoff) + `CAMPAIGN_PUBLIC_BASE_URL`=edgex constant. **Phase 2 (`7e6133c`, 2026-06-16, code-only no-migration):** admin manual result-override (`PATCH .../results/[matchId]`, source=manual/locked + Revert-to-ESPN), admin-only integrity-flag overlay (shared_phone/shared_name clusters, never on public path), config-driven Study Abroad Interest column. Migs 049/050 on shared DB. Prod-verified Phase 1.5 (200/masked/no-PII/404). **Hand the website dev:** `https://edgex.zunkireelabs.com/api/public/campaigns/<token>/leaderboard`. **Only open item (non-blocking):** Sadin's eyeball smoke of the Phase 2 override/revert/integrity UI on prod (override a result → re-rank; revert → next refresh re-fetches; integrity overlay shows the Padam Gaire `.com`/`.con` cluster). Leaderboard not final until the 2 pending WC matches finish + an admin Refresh (no cron in v1). Throwaway data: `/tmp/admizz-worldcup-*.csv`.

- [x] ~~**Dev auto-poll for inbound email**~~ **DONE 2026-06-02.** Added a root crontab on the VPS (`ssh vps`) running every 2 min: `curl -s -X POST -H 'Authorization: Bearer <dev INTERNAL_CRON_SECRET>' https://dev-lead-crm.zunkireelabs.com/api/internal/email/poll`. Replaces manual-curl-only (the GH Actions `email-poll.yml` cron only registers from the default branch `main`, so it never ran on dev). Verified end-to-end: cron logged in syslog, poll ran, `email_sync_state.last_synced_at` advanced, 0 errors. Inbound replies now appear in the CRM within ~2 min automatically. **For prod promotion:** add a parallel crontab line for the prod URL + prod secret. **Hardening (do for prod, optionally retrofit dev):** the cron line embeds the bearer token in plaintext → it shows in `/var/log/syslog`. Wrap the curl in a root-only script that reads the secret from a file so the token isn't logged. **Real-time option (future):** Gmail `users.watch()` + Pub/Sub push instead of polling — seconds latency, a separate phase.

- [x] ~~**Set `INTERNAL_CRON_SECRET` on dev** + `INTERNAL_CRON_SECRET_DEV` in GitHub repo secrets, **THEN smoke Email Phase 3 end-to-end**~~ **DONE 2026-06-01.** Secret generated (`openssl rand -hex 32`), written to dev `.env.local` via `ssh vps`, container force-recreated, and set as GH secret `INTERNAL_CRON_SECRET_DEV`. Smoke verified: bootstrap poll → `new_inbound_count:0` (correct); fresh reply → poll → `new_inbound_count:1` → rendered in CRM thread; reply-from-CRM threaded into same Gmail conversation (RFC reference chain grew 1→2→3, same `thread_id`, verified via psql). The 5-min GH Actions cron only registers once stage→main promotes (default-branch rule); until then dev polling is manual-curl. **Connect to the VPS with `ssh vps`, NOT the raw IP** (the raw-IP form fails password auth — see CLAUDE.md § Server).

- [x] ~~**For Phase 1 + 2 prod promotion**: add `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` to prod `.env.local`~~ **DONE 2026-06-04** — full-bundle prod promotion shipped (`aff7e22`, `stage→main` non-FF, deploy ✅). Added `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` (copied from dev — same Orca Auth OAuth client) + a fresh `INTERNAL_CRON_SECRET` to prod `.env.local` (backup `.env.local.bak-*`); prod deploy is `build --no-cache` + `up -d` (re-reads env_file — no force-recreate needed). Verified prod poll → `200 {accounts_polled:1, errors:0}`; KB/UTM routes exist + gated. **Hardened prod poll cron** added: `*/2 * * * * /root/poll-prod-email.sh` reading bearer from root-only `/root/.prod_email_cron_secret` (not in syslog). **Open: Sadin's prod smoke** (OAuth connect fresh inbox + send/reply loop + create KB + upload); **optional**: retrofit the DEV cron line to the same root-only-file pattern (its token is still plaintext in syslog).

- [x] ~~**Pre-existing latent bug: CRM integration lead-create never sets `pipeline_id`**~~ **RESOLVED 2026-06-08** (stage `b9c8320`). Fixed as part of the shared lead-intake resolver (Forms API audit P0 #1 / F1): `src/app/(main)/api/v1/integrations/crm/leads/route.ts` now calls `resolveLeadPipelineAndStage()` and sets `pipeline_id`. **Live-verified on dev** with a Prime Ceramics integration key — bare CRM POST lands in the tenant default pipeline (NON-NULL); explicit `stage_id` correctly derives its pipeline. Test rows cleaned, 0 real leads touched. On stage/dev only — rides the next stage→main promotion to prod.
