# Status Board — Lead Gen CRM

> Live checklist of open user-side actions, decisions, and questions. Companion to [SESSION-LOG.md](./SESSION-LOG.md). Update as items resolve.

Last updated: 2026-06-02 (Dev inbound email now **auto-polls every 2 min via a VPS root crontab** — replaced manual-curl-only; GH Actions cron never ran on dev because schedules register only from the default branch. Verified: cron fires, `last_synced_at` advances, 0 errors. See [reference memory] + the dev-auto-poll item below. Notifications + badges + unread-lead red dot all shipped to stage + deployed green. Email Phase 3 dev smoke ✅ COMPLETE — full bi-directional loop verified on dev; `INTERNAL_CRON_SECRET` set on dev + GH secret `INTERNAL_CRON_SECRET_DEV`. Notifications + attention-badges ✅ shipped to stage (`535c8ed`) — needs dev smoke (3 live-path items). Email Phase 2 ✅ + UTM tracking ✅ + Email Phase 3 ✅ all shipped to stage. Phase 3 closes the email-feature loop: counselor sends → recipient replies → ~5 min later inbound appears in CRM thread → counselor replies from CRM → recipient sees reply continue same Gmail thread. **NEW env var Phase 3 introduces — set BEFORE prod promotion**: `INTERNAL_CRON_SECRET` per env + `INTERNAL_CRON_SECRET_DEV` / `_PROD` as GH Actions secrets. Side-quest UTM merge (`6b9d741`) was Anish's `utm` branch cherry-picked onto current stage by Opus because his branch was 9 commits behind + had a 025 migration collision. Migrations 026 + 027 still need manual apply to dev DB before UTM features work on dev. Phase 1+2 prod promotion pending env-var setup + Sadin's go-ahead. Account 360 v2 live on prod at `0f58a0a`. **Focus: `education_consultancy`** — Email Phases 1✅, 2✅, 3 + 4 ahead. UTM live.)

---

## 🔴 Needs Sadin decision / action

- [ ] **Unified Inbox — Phases 1+2+3a on STAGE; real WhatsApp LIVE on dev; NOT on prod.** `stage` @ `0279241`. Full detail + dev wiring + prod checklist: `docs/UNIFIED-INBOX-BRIEF.md`. Real WhatsApp proven both directions on `dev-lead-crm` (inbound + outbound + read receipts). Dev is fully wired (Meta app published, permanent System User token, WABA subscribed, 4 env vars, `*/1` auto-drain cron). **Open items:** (1) **Phase 3b** — near-instant inbound (inline-process after fast-ack; brief `docs/UNIFIED-INBOX-PHASE-3B-BRIEF.md`); inbound lags up to ~60s on dev today (1-min cron). Sonnet brief ready. (2) **Prod promotion** (separate GO): merge stage→main (code-only, mig 044 already on shared DB) + set 4 WhatsApp env vars on prod + **add a prod-URL inbox drain** (the `inbox-process.yml` workflow targets the dev URL) + connect channel on prod + update Meta webhook to prod callback. (3) **Production WhatsApp prerequisites:** real **privacy policy page** (Opus can draft) + **real business number** + **Business Verification** (test number = 5 recipients only). (4) **Phase 4** Messenger/Instagram (adapters are stubs); **Phase 5** AI agent runtime.

- [ ] **SMOKE on `dev-lead-crm.zunkireelabs.com` — Edit Lead (inline, detail-page).** SHIPPED TO STAGE 2026-06-10 (`26dbe83`, `--no-ff` merge of `feature/lead-edit`; Opus-reviewed, both gates re-run: build clean, eslint 0 errors / 27 warnings). Brief archived at `docs/archive/features/LEAD-EDIT-BRIEF.md`. **Smoke matrix:** (1) On `/leads/[id]`, click **Edit** → name/email/phone (contact card) + city/country/preferred-contact/intake-source/campaign (key-info) flip to inputs together → change values → **Save** → toast "Lead updated", display reflects new values immediately, no refresh. (2) **Cancel** discards edits. (3) Bad email / short phone → inline error blocks Save. (4) Empty email AND first name → "at least an email or first name" error. (5) Edit the **email** → confirm `normalized_email` recomputed server-side (psql: `SELECT email, normalized_email FROM leads WHERE id='…'` — should match lowercased/trimmed). (6) Leads table: the row **⋯** now lives in a reserved slot **between the checkbox and the avatar**, appearing on **row hover only** (no trailing Actions column / no eye anymore — preview = the inline "👁 Preview" chip by the name). Hover a row → `⋯` fades in with no layout shift → **Edit** → navigates to `/leads/[id]?edit=1`, auto-opens edit mode, `?edit=1` stripped from URL after. Confirm `⋯` stays visible while its menu is open; confirm the inline Preview chip still opens the side panel. (7) As **it_agency** tenant (Zunkiree/Mobilise): edit mode also shows salutation/company-name/company-email/designation/prospect-industry inputs; as **non-it_agency** (Admizz/Arya): those are hidden. (8) **Add Lead** still works (now shares `validateLeadIdentity`) — no behavior change. (9) No regression to existing inline controls (stage/assign/tags/type/trip/package/professional), bulk toolbar, selection, Column Manager. **Deferred follow-up (non-blocking):** soft dedup warning on email-edit-to-existing (the backend `normalized_email` fix is done; only the optional UI warning was skipped).

- [ ] **Delete the stray dev `docker-compose.yml` on the prod box** (`/home/zunkireelabs/devprojects/lead-gen-crm/docker-compose.yml` is a DEV config — container `leads-crm-dev`, dev domain — sitting next to the correct `docker-compose.prod.yml`). Bare `docker compose ...` in that dir defaults to it and **clobbers prod** (caused a ~3-min prod outage on 2026-06-08 when reloading an env var; restored via `docker compose -f docker-compose.prod.yml up -d`). Until removed/renamed, NEVER run bare `docker compose` there — use CI/CD or `-f docker-compose.prod.yml`. Also remove the stray `scripts/undo-one-merge.ts` (throwaway swept into a commit). See the prod-compose-landmine memory.

- [ ] **Smoke Knowledge Bases on `dev-lead-crm.zunkireelabs.com`** (shipped to stage 2026-06-03, `db6bdc2`). Sadin already confirmed **create works** (the initial "Failed to create" was just the un-applied migration — mig `029` now applied + private bucket `knowledge-base-files` created on the shared DB). Remaining smoke: as Admizz admin, open a KB → upload a file (PDF) → row appears `ready` with size + card rollup updates; add a link + a note; download a file; rename/delete an item (file delete removes the blob); delete the KB (cascade + blobs gone). As a **non-admin / counselor**: list + detail are read-only (no New/3-dot/inline-edit), can open + download. As a **different tenant**: cannot see tenant 1's KBs. **Watch**: `.md`/`.csv` uploads — if the browser reports an empty/non-standard `file.type` they'll be rejected by the MIME allowlist (known minor). Universal feature → also confirm it shows + works for a **non-education** tenant (Zunkireelabs). If anything's off, draft a fixback brief.

- [x] ~~**Dev auto-poll for inbound email**~~ **DONE 2026-06-02.** Added a root crontab on the VPS (`ssh vps`) running every 2 min: `curl -s -X POST -H 'Authorization: Bearer <dev INTERNAL_CRON_SECRET>' https://dev-lead-crm.zunkireelabs.com/api/internal/email/poll`. Replaces manual-curl-only (the GH Actions `email-poll.yml` cron only registers from the default branch `main`, so it never ran on dev). Verified end-to-end: cron logged in syslog, poll ran, `email_sync_state.last_synced_at` advanced, 0 errors. Inbound replies now appear in the CRM within ~2 min automatically. **For prod promotion:** add a parallel crontab line for the prod URL + prod secret. **Hardening (do for prod, optionally retrofit dev):** the cron line embeds the bearer token in plaintext → it shows in `/var/log/syslog`. Wrap the curl in a root-only script that reads the secret from a file so the token isn't logged. **Real-time option (future):** Gmail `users.watch()` + Pub/Sub push instead of polling — seconds latency, a separate phase.
- [x] ~~**Set `INTERNAL_CRON_SECRET` on dev** + `INTERNAL_CRON_SECRET_DEV` in GitHub repo secrets, **THEN smoke Email Phase 3 end-to-end**~~ **DONE 2026-06-01.** Secret generated (`openssl rand -hex 32`), written to dev `.env.local` via `ssh vps`, container force-recreated, and set as GH secret `INTERNAL_CRON_SECRET_DEV`. Smoke verified: bootstrap poll → `new_inbound_count:0` (correct); fresh reply → poll → `new_inbound_count:1` → rendered in CRM thread; reply-from-CRM threaded into same Gmail conversation (RFC reference chain grew 1→2→3, same `thread_id`, verified via psql). The 5-min GH Actions cron only registers once stage→main promotes (default-branch rule); until then dev polling is manual-curl. **Connect to the VPS with `ssh vps`, NOT the raw IP** (the raw-IP form fails password auth — see CLAUDE.md § Server).
- [ ] **Smoke Notifications + attention-badges on `dev-lead-crm.zunkireelabs.com`** (Admizz admin + a counselor). The 3 live-path items not provable at code-review: **(1) email.received** — recipient replies to a connected inbox → manual poll (`curl -X POST -H "Authorization: Bearer <INTERNAL_CRON_SECRET>" https://dev-lead-crm.zunkireelabs.com/api/internal/email/poll`) → bell shows "New email reply" → click → lands on `/leads/{id}` → Emails sub-tab shows red unread badge → expand thread → badge clears + `emails.read_at` set (psql check). **(2) lead.created** — submit the public form → admin's bell gets "New lead" → "All Leads" sidebar badge increments → working the lead off `new` decrements. **(3) lead.stage_changed** — admin moves a counselor's lead → counselor notified; counselor moves their own lead → NO self-ping; move to a terminal stage (`enrolled`/`rejected`) → admins notified. **Self-suppression:** assign a lead to yourself → no notification. **Counselor scope:** as a counselor, `new_leads` badge counts only own assigned `new` leads; PATCH another counselor's thread `/read` → 404. **Non-education:** as Zunkireelabs admin, no email badges / no `email.received`; lead + stage notifications + "All Leads" badge still work. Migration `028` (`emails.read_at`) — confirm applied to dev DB (Sonnet reported applied + 2 rows backfilled; verify before smoke). Shipped at `535c8ed`. Generate the secret with `openssl rand -hex 32` (use the SAME value in both places so the GH Actions cron's bearer matches). After adding to `.env.local`: `docker compose up -d --force-recreate app`. Then trigger the GH Actions workflow manually via `workflow_dispatch` (Actions → "Email Poll (dev)" → Run workflow) to confirm 200; the scheduled cron then takes over every 5 min. Smoke matrix: (1) As Admizz admin, open a lead and send a fresh email to a test recipient (e.g. `<your-test-email>`) → Phase 2 path still works. (2) Recipient replies from their email client. (3) Within 5–10 min, refresh the lead's Emails sub-tab → inbound reply appears as part of the same `<EmailThreadCard>` (expanded shows both messages, blue bubble for inbound). (4) DB checks: new `emails` row direction='inbound' + thread_id matching + `gmail_message_id`/`rfc_message_id`/`in_reply_to`/`rfc_references` populated + `sender_user_id` NULL; `email_threads.message_count` incremented to 2 + `last_message_at` updated; `events` row `email.received` with payload; `email_sync_state.last_history_id` advanced + `last_error` NULL. (5) Click Reply on the thread → ComposeEmailDialog opens with From locked + To pre-filled + "Re: ..." subject. (6) Send the reply with fresh body. (7) Recipient receives in SAME Gmail thread (verify via In-Reply-To threading in their email client). (8) CRM thread shows 3 messages. (9) Counselor scoping: as a counselor user (not the one who sent), GET `/api/v1/email/threads?lead_id=X` returns [] for the lead. (10) Industry gating: as Zunkireelabs admin, no Compose CTA + `/email/threads` returns 403. (11) History expiration: `UPDATE email_sync_state SET last_history_id = '1'` → next poll bootstraps cleanly (last_error='history_expired_bootstrapped'). (12) Per-account error isolation: temporarily revoke OAuth on one inbox → next poll: that account logs error + increments `consecutive_error_count`; other accounts unaffected. Sonnet implements on `feat/email-phase-3-poll-reply` branched off `stage`. Opus reviews + squash-merges to stage. Phase 3 ships THREE coupled capabilities in one phase: (a) inbound polling via `/api/internal/email/poll` hit by GitHub Actions cron every 5 min, (b) full thread display via new `<EmailThreadCard>`, (c) reply-from-CRM via extending `<ComposeEmailDialog>` + `POST /api/v1/email/send`. Zero schema changes. Phase 3 closes when end-to-end loop works: counselor sends → recipient replies → reply lands in CRM within 5–10 min → counselor clicks Reply → recipient sees reply continue same Gmail thread. **NEW env var Phase 3 introduces**: `INTERNAL_CRON_SECRET` (generate per env with `openssl rand -hex 32`; add to `.env.local` on dev + GH repo secret as `INTERNAL_CRON_SECRET_DEV`; prod equivalents added during prod-promotion bundle). Without this, the polling endpoint fails-closed (401 on any request).
- [ ] ~~Apply migrations 026 + 027 to dev DB via Supabase MCP~~ **DONE 2026-05-31 night** — pre-check found both already applied (likely from Anish's development); full schema verification confirmed exact match to spec (form_configs.attribution column with default '{}'::jsonb; utm_links table with 3 FKs incl. cross-schema auth.users(id) ON DELETE SET NULL, RLS enabled, 3 policies present (no UPDATE per design), idx_utm_links_tenant_created index in place). Without these, `POST /api/v1/utm-links` and `GET /api/v1/utm-links` 500 (table missing), and form-builder reads/writes of the `attribution` field misbehave. Migration files: `supabase/migrations/026_form_config_attribution.sql` (1-line ALTER) + `supabase/migrations/027_utm_links.sql` (CREATE TABLE + RLS + index). Per CLAUDE.md, migrations don't auto-apply — they ship in the container but are applied manually. After apply, also queue them for prod when the Email + UTM prod promotion bundle goes out.
- [ ] **Decide CLAUDE.md "Developer Persona & System Guardrails" fate** (parked from utm merge). Anish added +41 lines of meta-rules near the top of CLAUDE.md in commit `5679eaa`: Plan Mode default, sub-agents liberally, "demand elegance", autonomous bug fixing ("do not prompt the user for direction unless..."), `lessons.md` pattern, rigorous verification. Some pieces (rigorous verification) are useful; some (autonomous-fix-no-prompting) conflict with our prod-merge-requires-explicit-approval discipline; the `lessons.md` pattern overlaps with what SESSION-LOG + STATUS-BOARD already do. Three options: keep all, drop all, or cherry-pick the bits that fit. Whatever you decide, bring it back as its own PR — not bundled with feature work. Also Anish's feature-table additions had one mislabel (`Contacts (prospects)` shown as education_consultancy; actually `FEATURES.CRM_CONTACTS` is it_agency) — fix if we revisit.
- [ ] **Smoke UTM feature on `dev-lead-crm.zunkireelabs.com`** (Admizz admin, AFTER migrations applied). (1) Visit a published form URL with `?utm_source=newsletter&utm_medium=email&utm_campaign=spring2026` → submit → confirm new lead has `intake_source/medium/campaign` populated correctly. (2) Same submit but with no URL params → confirm intake_* falls back to per-form `attribution` defaults if set, else to "form"/null. (3) `/forms/utm-builder` → build a UTM link with form picker + utm fields → Save → confirm row appears in saved list → Delete → confirm removal. (4) Dashboard `/dashboard` → confirm `<UtmAnalyticsSection>` charts render (3 bar charts: source/medium/campaign) → click a bar → confirm cross-filter. (5) As Zunkireelabs admin → `/forms/utm-builder` should 404 or 403 (industry-gated FEATURES.FORM_BUILDER); dashboard UtmAnalyticsSection NOT rendered. (6) As counselor → `/forms/utm-builder` returns 403 (admin-only on the POST).
- [ ] **Smoke Email Phase 2 on `dev-lead-crm.zunkireelabs.com`** (Admizz admin). Per the verification matrix at the bottom of the archived brief (`docs/archive/features/EMAIL-PHASE-2-BRIEF.md`): open a lead → Activity tab → Emails sub-tab → Compose Email → From dropdown shows connected inboxes (3-state UI: 0 / 1 disabled / 2+ enabled) → fill To (defaults from `lead.email`) + Subject + Body (TipTap toolbar Bold/Italic/Link/Bullets/Numbered) with optional `{{first_name}}`/`{{last_name}}` merge fields → Send → recipient receives a real Gmail email from the connected address → toast success → modal closes → sent row prepends with ✉ Sent badge within 1 sec. DB checks: `email_threads` row created, `emails` row with `direction='outbound'` + `gmail_message_id` + `rfc_message_id` set, `events` row `email.sent` with full payload. Counselor-scoping check: as a counselor on the same lead, GET `/api/v1/email/threads?lead_id=X` returns only their own sent emails. Industry-gating check: as Zunkireelabs admin, Compose CTA NOT visible on Emails sub-tab AND POST `/api/v1/email/send` returns 403. Token-refresh persistence check: manually expire `token_expiry` in DB → send → confirm `access_token` + `token_expiry` updated post-send (Fix 3 from the review).
- [x] ~~**For Phase 1 + 2 prod promotion**: add `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` to prod `.env.local`~~ **DONE 2026-06-04** — full-bundle prod promotion shipped (`aff7e22`, `stage→main` non-FF, deploy ✅). Added `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` (copied from dev — same Orca Auth OAuth client) + a fresh `INTERNAL_CRON_SECRET` to prod `.env.local` (backup `.env.local.bak-*`); prod deploy is `build --no-cache` + `up -d` (re-reads env_file — no force-recreate needed). Verified prod poll → `200 {accounts_polled:1, errors:0}`; KB/UTM routes exist + gated. **Hardened prod poll cron** added: `*/2 * * * * /root/poll-prod-email.sh` reading bearer from root-only `/root/.prod_email_cron_secret` (not in syslog). **Open: Sadin's prod smoke** (OAuth connect fresh inbox + send/reply loop + create KB + upload); **optional**: retrofit the DEV cron line to the same root-only-file pattern (its token is still plaintext in syslog).
- [ ] **IT-agency surface pass — PAUSED, RESUMABLE.** Resume when education_consultancy work feels done (or whenever Sadin pivots back). Done so far in this pass: `/contacts` list + `/contacts/[id]` 360° detail, `/accounts` list + `/accounts/[id]` 360° detail (v1 + v2), `/projects` Board view, sidebar nav grouping. **Remaining when we resume**: `/dashboard` · `/leads` list (already mostly the reference) · `/team` · `/time-tracking` · `/time-tracking/approvals` · `/settings` · `/projects` Tasks/Members/Table views (round out). Same workflow per surface: Opus audits → writes brief ending with the Sonnet handoff prompt → Sadin pastes the prompt → Sonnet implements → Opus reviews + stage merge → Sadin smokes. **Possible `/projects` follow-ups** when resumed: `+ New Project` button, Sort popover, Export CSV. **Possible `/contacts/[id]` v2 follow-ups**: `contact_notes` table + timeline composer, `contact_activities` audit log, Log Meeting persistence, last interaction date computed field. **Possible `/accounts/[id]` v3 follow-ups (deferred from v2)**: sparkline / trend chart of billable $ over last 6 months on KEY INFO; Billing tab content (invoices + retainer + per-project breakdown — needs invoices model); filter chips on Activity tab ("All / Time / Projects / Contacts / Changes"); per-user billable detail page (drill-in from Account Team row); `last_activity_at` computed field on /accounts list; at-risk health score (needs defined signals — no activity 14d, missed deadlines, etc.); MRR/retainer model. **Possible sidebar follow-ups**: localStorage persistence of collapse state, retrofit aria-expanded on Public Forms toggle.
- [ ] **Eyeball items pending Sadin's smoke** (none blocking, all post-merge polish judgment): (1) selected row in dropdowns has zero background at rest now — only the radio-circle + check signals selection; pipeline selector with multiple pipelines is the test surface; (2) Pipeline "Default" badge is now neutral gray — if hard to spot in long lists, follow-up could give it soft amber or purple; (3) table-row hover (`bg-gray-50`) vs dropdown hover (`#0000170b`) intentionally different — could unify in a follow-up; (4) Status filter chip on `/contacts` + `/accounts` always renders "engaged" since default value `"active"` ≠ `"all"` and FilterDropdown's `isActive` counts anything-other-than-`"all"` as active — visual quirk, not a bug; (5) on `/accounts/[id]`: admin sees primary contact name as a popover trigger (no link to `/contacts/[id]`); viewer sees a Link. Inconsistent but the popover IS the admin's main action — could revisit if it bothers in smoke; (6) on `/accounts/[id]`: status dots on HealthSnapshotCard use native `title` attribute, not Radix Tooltip — works but less polished, brief allowed it. **(7) NEW from v2**: Activity row "name" renders as `email.split('@')[0]` (lowercase, e.g. "alice logged 6.5h on «Project X»"). Acceptable for v2; polish pass could capitalize or render first+last name. **(8) NEW from v2**: Counselor scoping on /activity events — project events are NOT counselor-scoped since projects don't have a counselor-ownership concept. Defensible; no fix planned. **(9) NEW from v2**: Activity feed "Load more" effectively caps at ~7 pages of data (events ≤100 + time_entry events ≤50 + derived ≤100 = ~200 items merged). Brief did not require deep pagination; acceptable for v2. **(10) NEW from v2**: `/team` endpoint does N+1 `getUserById` calls per team member. Brief allowed for <10-person teams; revisit if a real account has >20 contributors.
- [ ] **3 grey-in-white surfaces** flagged during chrome restyle will likely get repainted during the styling pass: `/pipeline` kanban lane backgrounds, `/projects` workspace full-bleed grey, `/leads` table chrome (this last one partially touched by the table text bump but the table wrapper bg wasn't audited). Each paints its own grey bg → reads as grey-card-inside-the-white-card. Resolve in-flight as the respective dashboard pages come up.
- [ ] **Smoke on the 2026-05-28 production bundle is still technically open** but Sadin pivoted before running it. Most of what would have been smoked (chrome restyle, Members view, tag picker, Phase 5 shortcuts, Orca/Ops tabs) gets re-touched during the styling pass anyway. Decide post-styling-pass whether to retire the standalone smoke item or run it once the pass closes.
- [ ] **Phase 4 + 4.5 Time Tracking smoke gaps** (carryover): shipped on visual-confirmation. Not-yet-verified: bulk approve/reject, non-admin member view, Admizz 404 on /time-tracking, CSV export contents, TOCTOU race two-window test. No production regression evidence — pure thoroughness backlog.
- [ ] **Counselor password (`manjila@zunkireelabs.com`) still rotated** from Time Tracking Phase 5 verification. Admizz admin restored to `admizz123` on 2026-05-27. Future smoke runs that need counselor scoping require a fresh reset via service-role admin API — ask Sadin first since this locks out a real teammate.
- [ ] **Branch cleanup (optional, low priority).** 4 dangling already-merged branches safe to delete: `check-in`, `consultancy-update`, `create-form`, `tags` — all zero-ahead of stage. **`feature/ai-orchestrate-orca` (Sadin, 2026-04-10, stale UI shell) — its 6-screen Orca UI shell was HARVESTED verbatim to stage 2026-06-04 (`43b074e`, see SESSION-LOG); the branch's later phases (mig `009`, `/api/v1/orca/*`, agent wiring) were deliberately NOT taken. Decide whether to keep it for that unported history or delete it now that the surface is live.** Also deletable post-promotion: `feat/orca-ui-shell` + `feat/org-structure` (verify merged first).

## 🟢 Code-review checklist additions (design pass postmortems)

- [ ] **Page-padding stacks with the dashboard shell**: when restyling a page, check the outer page wrapper's padding against `src/components/dashboard/shell.tsx:409` — the shell already wraps page content in `p-4 mr-4 mb-4`. Pages that add their own `p-4` / `p-6` / `p-8` on the outermost wrapper **stack** that padding on top of the shell's, producing an inset that's 2× what was intended. Reference page that intentionally relies on the shell only: `/pipeline` (`src/app/(main)/(dashboard)/pipeline/page.tsx:80`). Pages that historically added their own padding and now mismatch the shell (fixed): `/projects` (`workspace.tsx`, fixed in `2aa45df`). Same rule applies to top-margin and side-padding. Hit on 2026-05-28 PM: `/projects` was double-padding to 40px-from-edge vs Pipeline's 16px — invisible in code, glaring in screenshots side-by-side.

## 🟢 Code-review checklist additions (Phase B + C + D postmortems)

- [ ] **PostgREST embed FK disambiguation**: any time a migration adds a reverse FK between two tables that already have a forward FK, every `.select("*, OtherTable(...)")` between those tables MUST use the explicit FK name (e.g. `accounts!contacts_account_id_fkey(id, name)`). Latent bug hidden until the first embed query runs. Grep for `select.*${otherTable}\\(` whenever adding a reverse FK.
- [ ] **PATCH preserves POST invariants**: any field-level invariant enforced on POST (e.g. "at least one of email or phone required") MUST also be enforced on PATCH. Fetch the existing row, compute the resulting state with the patch applied, validate.
- [ ] **New page components need a route shell**: when scaffolding a new UI component (e.g. ContactDetailPage), the corresponding Next.js page shell at `src/app/.../[id]/page.tsx` MUST be created in the same phase, even if the component is just a placeholder. "Exported but not wired yet" is not a valid phase-A state.
- [ ] **`.select()` after insert/update**: the return shape must match what the UI consumes for optimistic adds. If the read endpoint joins on `accounts(id, name)`, the insert endpoint must too — else freshly-created rows show with empty join columns until refresh.
- [ ] **Radix Select forbids empty-string `<SelectItem value="">`**: Radix UI reserves `value=""` for "clear selection / show placeholder" and throws at render time. Use a sentinel string (e.g. `"__none__"`) and map it to null/undefined at submit. Same constraint applies to shadcn's Select (built on Radix). Not caught by TypeScript or lint — runtime-only error. Applies to any future picker UI.
- [ ] **Cross-cutting predicate audits must grep the whole repo**: when a feature introduces a new "soft state" filter (e.g. `WHERE converted_at IS NULL`, `WHERE deleted_at IS NULL`), DON'T trust a hand-curated targets list — grep `from("TableName")` across `src/` and audit EVERY hit. Phase D's filter audit punted on `/api/v1/pipelines` and `/api/v1/pipelines/[id]` because the planning prompt didn't enumerate them; the kanban (via queries.ts) was filtered but the pipeline selector dropdowns (via API) weren't, producing inconsistent UI counts. Fix landed as `11a3460`. Same class of bug as forgetting to filter `deleted_at` in a query.

## 🟢 Code-review checklist additions (from Phase B postmortem)

- [ ] **PostgREST embed FK disambiguation**: any time a migration adds a reverse FK between two tables that already have a forward FK, every `.select("*, OtherTable(...)")` between those tables MUST use the explicit FK name (e.g. `accounts!contacts_account_id_fkey(id, name)`). Latent bug hidden until the first embed query runs. Grep for `select.*${otherTable}\\(` whenever adding a reverse FK.
- [ ] **PATCH preserves POST invariants**: any field-level invariant enforced on POST (e.g. "at least one of email or phone required") MUST also be enforced on PATCH. Fetch the existing row, compute the resulting state with the patch applied, validate.
- [ ] **New page components need a route shell**: when scaffolding a new UI component (e.g. ContactDetailPage), the corresponding Next.js page shell at `src/app/.../[id]/page.tsx` MUST be created in the same phase, even if the component is just a placeholder. "Exported but not wired yet" is not a valid phase-A state.
- [ ] **`.select()` after insert/update**: the return shape must match what the UI consumes for optimistic adds. If the read endpoint joins on `accounts(id, name)`, the insert endpoint must too — else freshly-created rows show with empty join columns until refresh.
- [ ] **`PRICING.md` at repo root**: duplicate of `docs/reference/PRICING.md`. Delete the root copy (recommended).

## 🟢 Email feature setup gaps (one-time reference for prod rollout + future tenant onboarding)

Documenting the env-setup steps NOT covered in the Phase 1 brief that surfaced during the dev smoke 2026-05-31 evening. Future setups (prod rollout; Anish onboarding; new education_consultancy tenant) should follow this checklist BEFORE expecting the email feature to work:

1. **OAuth client setup in Google Cloud Console** — credentials don't exist by default. Per-environment-or-shared:
   - Pick or create a Google Cloud Project (today: `Orca Auth`, project ID `370797703920`).
   - APIs & Services → Library → search "Gmail API" → **Enable** (project-level; once enabled all OAuth clients in the project can call Gmail API).
   - Google Auth Platform → Clients → **+ Create client** → Application type: Web application → Name: `edgeX CRM` (or new name for separate environments) → Authorized redirect URIs: 4 URIs (2 envs × 2 endpoints):
     - `https://dev-lead-crm.zunkireelabs.com/api/v1/email/inboxes/callback`
     - `https://lead-crm.zunkireelabs.com/api/v1/email/inboxes/callback`
     - `https://dev-lead-crm.zunkireelabs.com/api/v1/settings/email-accounts/gmail/callback`
     - `https://lead-crm.zunkireelabs.com/api/v1/settings/email-accounts/gmail/callback`
   - Save the Client ID + Client Secret IMMEDIATELY (secret is shown once).
2. **OAuth consent screen settings**:
   - Audience → User type = **External** (not Internal — counselors at external orgs need to OAuth).
   - Publishing status: **Testing** is fine for Phase 1/2 smoke (≤100 test users). For real Admizz counselor rollout: add each counselor email to "Test users" OR Publish app (External + Production, requires Google verification, 1-2 weeks).
3. **Server env**: add to `.env.local` on EACH environment (dev: `/home/zunkireelabs/devprojects/lead-gen-crm-dev/.env.local`; prod: `/home/zunkireelabs/devprojects/lead-gen-crm/.env.local`):
   ```
   GOOGLE_CLIENT_ID=<client-id>.apps.googleusercontent.com
   GOOGLE_CLIENT_SECRET=<client-secret>
   ```
4. **Container recreate (NOT restart)**: `docker compose up -d --force-recreate app` reads `env_file`; `docker compose restart` does NOT. Skipping force-recreate = silent stale env = silent 503 from `/api/v1/email/inboxes/connect`.
5. **OAuth Secret Rotation Pattern**: Google Cloud Console → edgeX CRM → "+ Add secret" → both old + new are valid simultaneously (zero downtime) → update `.env.local` with new secret → force-recreate container → verify Connect works → come back and delete old secret. This is the safe rotation path; useful both for periodic rotation AND for emergency rotation if a secret leaks.

When promoting Email phases to prod, run through this checklist for the prod environment before triggering the merge.

## 🟡 Open questions

- [ ] `docs/archive/research/ai-insight-*` (2026-03-28) — `013_lead_insights` migration + `/api/v1/ai/chat` stub suggest partial implementation. Is the AI insight feature still on the roadmap, or are the stubs dormant? The new `industries/<id>/ai/agent.ts` slots are reserved for per-industry prompts — when this work gets prioritized, that's the home.
- [ ] `docs/feature/email-automation/` was empty when the docs were reorganized — the actual email-forward + Gmail OAuth feature shipped via `f728ca8` (May 4). Was the empty dir a stale placeholder? Safe to confirm-and-delete.
- [ ] Older planning docs `docs/archive/plans/enhanced-dashboard.md` and `lead-detail-redesign.md` (2026-03-27) — were these the briefs for PRs #4–#5? Worth retitling/dating if so, or moving to `archive/stale/` if superseded.
- [ ] **Promote tags UI to `_shared/` when a 2nd industry wants tags**. Today Student/Parent labels are hardcoded in education's check-in UI. When IT or another industry wants tags, move the UI to `src/industries/_shared/features/lead-tags/` and let each manifest define its own label set (Student/Parent vs Hot/Warm/Cold vs Buyer/Seller etc.) via per-industry config. Not blocking.

## 🟠 Ongoing hardening work (not blocking)

- [ ] **Deploy-staging.yml silent-failure on Docker build.** The 2026-05-31 evening Phase 1 deploy hit a `docker compose build --no-cache` OOM (FATAL: heap allocation failure at 2GB cap; Dockerfile was running with `--max-old-space-size=2048`) but the workflow still reported "success" and the dev container kept serving stale pre-Phase-1 code. Two contributing factors: (a) `appleboy/ssh-action`'s script wrapper apparently doesn't reliably propagate non-zero exit from `docker compose build` despite `set -e`; (b) the Verify step (`curl /login`) returned 200 against the stale image — false positive (the new email API routes 404'd but /login exists in both old and new code). Fix landed for (a) via heap bump in `2952a3f`. **Open hardening**: harden the deploy script in `.github/workflows/deploy-staging.yml` (and mirror in `deploy.yml`) — wrap `docker compose build --no-cache` in an explicit `|| { echo "BUILD FAILED"; exit 1; }`, and harden the Verify step to hit at least one route that's known-new-in-this-commit (e.g., a build-info endpoint that returns the deployed commit SHA — could be a 1-liner GET). Without this, any future OOM or build failure could silently leave the dev/prod container on stale code with a green deploy badge.
- [ ] **New `DELETE /api/v1/email/inboxes/[id]` should check `email_forward_rules.email_account_id` FK references before deleting.** The legacy `DELETE /api/v1/settings/email-accounts/[id]` returns 409 if rules use the account; the new endpoint does not. Theoretical for Phase 1 (no email-forward rules will be created against per-user inboxes); the FK is `ON DELETE SET NULL` so the worst case is a rule's `email_account_id` silently becomes NULL on disconnect. Add the equivalent check in Phase 2 or 3 — mirror lines 37-46 of `src/app/(main)/api/v1/settings/email-accounts/[id]/route.ts`.
- [ ] **(Optional second pass) Serialize stage + main deploys via a shared concurrency group.** The SSH `command_timeout` fix (`5ce03d2`) gives both pipelines 30m headroom, so dual-deploy contention is no longer fatal — but the contention itself still happens (both builds running on the same SSH host slow each other down). If we want to eliminate the slowdown entirely, unify `concurrency.group` across `deploy-staging.yml` and `deploy.yml` (e.g., `group: deploy-ssh-host`). Trade-off: a stage push during an in-flight prod deploy would explicitly queue and wait. Wait-and-see — if no further contention timeouts after the 30m bump, this can stay parked.
- [ ] **Migrate ~33 legacy authenticated routes to `scopedClient(auth)`**. Today they use raw `createServiceClient()` + manual `.eq("tenant_id", auth.tenantId)`. Four routes migrated as proof (`/api/v1/team` GET + DELETE, `/api/v1/notifications` GET). The wrapper handles tenant filter auto-injection + strips caller-supplied `tenant_id` from update/insert payloads; new routes must default to it. Migrate at the rate of "every route I'm already editing for another reason" — no big-bang refactor.
- [ ] **Wire `events` table → webhook dispatcher**. Mutations emit to `events`; the dispatcher exists but isn't consuming. Until then, webhook delivery is broken.
- [ ] **Build per-industry AI agent prompts/tools**. The `industries/<id>/ai/agent.ts` slots exist and the manifest type reserves an `ai` field, but no real prompts/tools are wired. Education-consultancy first when prioritized.
- [x] ~~**Pre-existing latent bug: CRM integration lead-create never sets `pipeline_id`**~~ **RESOLVED 2026-06-08** (stage `b9c8320`). Fixed as part of the shared lead-intake resolver (Forms API audit P0 #1 / F1): `src/app/(main)/api/v1/integrations/crm/leads/route.ts` now calls `resolveLeadPipelineAndStage()` and sets `pipeline_id`. **Live-verified on dev** with a Prime Ceramics integration key — bare CRM POST lands in the tenant default pipeline (NON-NULL); explicit `stage_id` correctly derives its pipeline. Test rows cleaned, 0 real leads touched. On stage/dev only — rides the next stage→main promotion to prod.
- [ ] **Address remaining low-severity code-review findings**: typed-as-`keyof typeof INDUSTRY_ICONS` for icon names (catch typos at compile time), runtime warning when scopedClient.insert silently overrides caller tenant_id, telemetry for industries-not-in-registry. None are bugs today; all worth doing on the next sweep.

## ✅ Recently resolved

- 2026-05-31 night — **Email Phase 3 (inbound polling + thread display + reply-from-CRM) shipped to stage** (`6acd9f8`, squash from `feat/email-phase-3-poll-reply`). 13 files / +1,215 / -238 (includes 2 orphan deletes for Phase 2's `use-sent-emails.ts` + `sent-email-card.tsx`, superseded by threaded versions). Email feature is now bi-directional. **Three coupled capabilities**: (1) inbound polling via `/api/internal/email/poll` hit by `.github/workflows/email-poll.yml` GH Actions cron every 5 min — bearer-auth via INTERNAL_CRON_SECRET, fail-closed if env unset, per-account try/catch + Promise.allSettled chunks of 5, DB-level industry gate filters to education_consultancy only, history API 404 bootstraps from current historyId, idempotency via unique index, emits `email.received` events; (2) thread display via new `<EmailThreadCard>` replacing `<SentEmailCard>` — collapsed shows subject + participants + count + last-activity time + direction badge, expanded shows messages oldest→newest with inbound/outbound visually distinct, Reply button at bottom; `/api/v1/email/threads` rewritten to return embedded `emails(...)` shape via PostgREST and counselor scoping changed from broken `sender_user_id` (NULL on inbound — silently dropped every received message) to 2-query own-accounts approach; (3) reply-from-CRM via extending `<ComposeEmailDialog>` with `replyContext` prop that locks From picker to thread's account + pre-fills To from message-being-replied-to + "Re: ..." subject, and extending `POST /api/v1/email/send` with optional `reply_context` validating same-account constraint (400 `REPLY_ACCOUNT_MISMATCH` on mismatch) and reusing existing thread; `sendMessage` wires Phase 2's previously-stubbed `threadId` + `inReplyTo` + `references` (Gmail groups via threadId, recipients' clients via In-Reply-To/References). NO schema changes — Phase 1's mig 025 had everything needed. All 6 of Sonnet's self-flags verified clean — no fixback round needed. Local gates clean (build, all 6 email routes + new polling endpoint register; ESLint at 17 baseline). All 9 code-review checklist items satisfied (including 2 new this phase: bearer-secret fail-closed, sync-state concurrency safety). 4 minor non-blocking observations logged in SESSION-LOG dated entry for future polish. Brief archived at `docs/archive/features/EMAIL-PHASE-3-BRIEF.md`. Stage deploy in flight at time of this docs commit. Awaiting Sadin's INTERNAL_CRON_SECRET setup + dev smoke before considering Phase 1+2+UTM+3 prod-promotion bundle.
- 2026-05-31 night — **UTM tracking (Anish's `utm` branch) rebased + shipped to stage** (`6b9d741`, squash from a fresh `feat/utm-rebased` branch). 27 files, +1,385 / -62. 2 new migrations (026_form_config_attribution + 027_utm_links — renumbered from utm's original 025/026 to avoid collision with Phase 1's email foundation). What ships: UTM auto-capture from form URL (gated to education_consultancy, falls back to per-form attribution defaults then "form"), per-form `<AttributionEditor>`, `/forms/utm-builder` admin page + saved UTM links (3 API endpoints, all industry-gated FEATURES.FORM_BUILDER, admin-only on mutations, scopedClient, validates form_id tenant ownership), dashboard `<UtmAnalyticsSection>` with cross-filtering bar charts (reads `lead.intake_*`), `lead-name.ts` cross-cutting util (fullname/initials with custom_fields fallback). Bundled non-UTM change: dashboard's duplicate LeadsTable removed (canonical surface is `/leads`). Anish branched off stage before Email Phase 1 shipped and never rebased — raw merge would've wiped Phase 1+2 and caused a 025 migration collision. Opus cherry-picked just the UTM-feature files onto current stage (11 straight-take files + 2 manual merges for lead-tabs.tsx and types/database.ts); 9 categories of files deliberately NOT applied to preserve Email Phase 1+2 + Docker heap + email registry/manifest/settings entries. Anish's original `utm` branch preserved on origin for credit + audit. Local gates clean (build, all 6 email + both UTM routes register; ESLint at 17 baseline). CLAUDE.md additions Anish made (Developer Persona section + feature-table additions) parked for separate decision. Migrations need manual apply to dev DB before features work — top STATUS-BOARD action.
- 2026-05-31 evening — **Email Phase 2 (compose + send + log on lead detail) shipped to stage** (`977fc44`, squash from `feat/email-phase-2-send`). 14 files, +1,642 / -38. No schema changes (Phase 1's mig 025 sufficient). The email feature is now usable end-to-end for education_consultancy. New `POST /api/v1/email/send` (industry-gated, `scopedClient`, from-account ownership check returns 403 not 404, server-side merge-field interpolation BEFORE Gmail call so stored content matches sent, counselor-scoped lead lookup for merge fields, fire-and-forget refreshed-token persistence) + `GET /api/v1/email/threads?lead_id=X` (industry-gated, scopedClient, counselor-scoped via sender_user_id, PostgREST embed via `email_threads!inner` with explicit tenant filter on join column). `gmail-client.ts` gains `sendMessage()` using `googleapis.gmail.users.messages.send` + `nodemailer/lib/mail-composer` for RFC 822; Message-ID set explicitly as `<uuid@edgex-crm.com>`; calls `refreshAccessTokenIfNeeded` first and reuses the refreshed access_token via `setCredentials` to skip a second OAuth roundtrip. 4 new UI components (`<ComposeEmailDialog>`, `<FromAccountPicker>` with 3 distinct states, `<TipTapEditor>` controlled wrapper using TipTap v3 options-object API, `<SentEmailCard>`) + 2 new hooks (`useConnectedInboxes` shared with refactored `<InboxConnector>`, `useSentEmails`). `<ActivitiesPanel>` Emails sub-tab evolved to a combined list (sent emails ✉ + logged emails 📝, sorted desc by date); Compose CTA gated by `industryId === "education_consultancy"`; compose modal dynamically imported so TipTap (~150KB) doesn't affect `/leads/[id]` first paint. First Sonnet push had 4 review findings caught (TipTap silently uncontrolled / scopedClient skipped with wrong rationale / refreshAccessTokenIfNeeded defined-but-never-called / counselor scoping gap on merge-field lead lookup); all 4 fixed in `80e3232` before squash. Local gates green (build clean, ESLint at 17 baseline). All 7 code-review checklist items considered (PostgREST embed FK ✓, .select after insert ✓, Radix Select sentinel ✓, cross-cutting predicate audit done). 3 minor non-blocking observations logged on STATUS-BOARD for follow-up (TipTap empty-state setContent runs per render harmlessly; fresh-token path misses cached access_token micro-optimization; `<SentEmailCard>` body_html DOMPurify hardening for Phase 3/4). Brief archived at `docs/archive/features/EMAIL-PHASE-2-BRIEF.md`. Stage deploy in flight at the time of this docs commit. Awaiting Sadin's dev smoke before considering Phase 1+2 prod promotion bundle.
- 2026-05-31 evening — **Email Phase 1 SMOKE VERIFIED on dev**. Connect Gmail roundtrip works end-to-end on `dev-lead-crm.zunkireelabs.com` for Admizz admin. Connect → Google consent → callback → `connected_email_accounts` row populated with `user_id` → `<InboxConnector>` renders the row. Disconnect/Reconnect cycle works. Industry-gated correctly. Setup friction documented in the "Email feature setup gaps" section above (4 things weren't in the Phase 1 brief: Gmail API enablement at project level; OAuth client creation in Google Cloud Console; force-recreate vs restart for env reload; secret rotation pattern). OAuth secret rotated post-smoke (transcript-leaked secret killed via Google Cloud Console "+ Add secret" then delete old). Phase 2 brief written at `docs/EMAIL-PHASE-2-BRIEF.md` (625 lines, covers compose modal + send endpoint + threads endpoint + sub-tab evolution; no schema changes; Sonnet handoff prompt at bottom).
- 2026-05-31 evening — **Email Phase 1 (foundation) shipped to stage** (`c9db7c2`, squash from `feat/email-phase-1-foundation`). 15 files, +887 / -31. Closes Phase 1 of the approved 4-phase Email feature plan. Foundation only: migration 025 (3 new tables — `email_threads`, `emails`, `email_sync_state` — + 1 alter on `connected_email_accounts` adding `user_id` and shifting RLS from tenant-admin-scoped to user-scoped with tenant-admin SELECT for oversight); `googleapis` package install; `FEATURES.EMAIL` constant registered in education_consultancy manifest (no sidebar entry); `gmail-client.ts` in feature folder exporting 3 Phase 1 functions (`createOAuth2Client`, `getProfileEmail`, `refreshAccessTokenIfNeeded` — no send/list/get yet); evolved legacy `/settings/email-accounts/gmail/callback` to capture `user_id` from session keeping the NOT NULL invariant satisfied; 4 new `/api/v1/email/inboxes/*` endpoints (industry-gated, HMAC-signed state, `prompt=consent` for multi-inbox refresh tokens); `<InboxConnector>` client UI on `/settings` (conditionally rendered for education_consultancy, wrapped in `<Suspense>`). Reviewed against brief + 7-item checklist + verification matrix + ran local gates myself (build clean, ESLint at baseline 17 warnings). Sonnet's two self-flagged concerns (SameSite=Lax + RLS coverage) both theoretical; no fixback needed. One hardening backlog item logged above (new DELETE should check `email_forward_rules` FK). Phase 1 brief archived at `docs/archive/features/EMAIL-PHASE-1-BRIEF.md`. Stage deploy in flight at the time of this docs commit.
- 2026-05-31 evening — **Production promotion shipped** (`0f58a0a`, non-FF ort merge stage→main). Account 360 v2 + docs now live on `lead-crm.zunkireelabs.com`. 4 stage commits land on production: v2 brief (`628ff0a`), v2 squash (`0ec69a1`), v2 ship docs (`82e4282`), pivot docs (`426a97a`). 22 files, +1,616 / -49. Deploy completed clean in 4m23s. No concurrent stage deploys at promotion time — the 30m SSH timeout fix wasn't tested again. Pre-flight discipline held: stage deploy of `426a97a` confirmed "completed" before pushing; main-only operational commits (5 prior promotions + Anish merge + CI nudge) blocked fast-forward; ort merge resolved zero conflicts. Live smoke: `/login` 200 in 381ms, `/accounts` + `/contacts` + `/pipeline` all 307 (auth redirects). Visual smoke deferred to Sadin (Zunkireelabs admin on `/accounts/[id]` should see 3 billable rows in KEY INFO + Team card between Health and OpenLeads + populated Activity tab).
- 2026-05-31 afternoon — **`/accounts/[id]` v2 shipped to stage** (`0ec69a1`, squash from `feat/account-detail-360-v2`). Closes the 3 explicit deferrals from v1 (`d1a4b89`): billable totals in KEY INFO (this month $ + ▲/▼/—/New delta pill vs last month + this month hrs + lifetime $), Account Team card slotted between Health and OpenLeads (two-group Owners/Contributors with role pills per CRM-expert PSA pushback, 90-day contributor filter), Activity tab content (events table UNION derived time-logged stream aggregated by user+day+project, paginated 30/page). 18 files, +1,080 / -35. 3 new API endpoints (`billable-summary`, `team`, `activity`); 5 small `emitEvent()` additions on existing PATCH/approve/reject/convert routes; 2 new utils (`format-billable-delta`, `format-relative-time`). One fixback caught at review (rolled into the squash): `time_entry.approved`/`time_entry.rejected` events were being silently dropped from the Activity feed because the events query filtered `.in("entity_id", allEntityIds)` and time_entry IDs were never in that list. Root cause was a gap in the v2 brief (Opus owned it). Fix added a third parallel query filtered by `entity_type="time_entry"` + `.contains("payload", { account_id: id })`, merged into eventItems before sort/paginate; counselor scoping via `payload->>user_id`. Same fixback also replaced silent `loadMore()` catch with `toast.error()`. Build + ESLint local gates clean (17 warnings, baseline preserved). All 7 code-review checklist items considered. Workflow snag (recovered): Sonnet's fixback commit was made locally in the shared clone but never pushed to `origin/feat/account-detail-360-v2` — squash-merge picked up the local commit anyway. Lesson: when Sonnet operates in the same shared clone (not a worktree), verify `git log origin/<branch>..HEAD` vs `git log <branch>..HEAD` before assuming what's on remote. Brief archived. V3 deferrals listed in "Pick next" item above.
- 2026-05-31 — **Production promotion shipped** (`7863473`, non-FF ort merge stage→main). 8 stage commits land on production: Contact 360 detail (`4890488`), Sidebar nav grouping (`b6219b2`) + truncate polish (`6abeac0`), Account 360 detail (`d1a4b89`), 4 docs commits. Deploy completed clean in 4m17s. Live smoke: `/login` 200 in 318ms, `/accounts` + `/contacts` + `/pipeline` all 307 (auth redirects). The 30m SSH timeout fix wasn't tested — no concurrent stage deploys at promotion time. Pre-flight discipline held (verified expected commits both ways before merging). 6 main-only operational commits (resume said 5 — extra Anish merge noted, no app code to preserve). Non-FF ort merge resolved zero conflicts. CRM-expert-informed design pass second wave now live on `lead-crm.zunkireelabs.com`. Workflow split worked clean end-to-end (Opus plans + reviews + ships docs; Sonnet writes feature code; fixback caught at review and routed back to Sonnet, not edited directly by Opus).
- 2026-05-29 evening — **`/accounts/[id]` 360° detail page shipped to stage** (`d1a4b89`, squash from `feat/account-detail-360`). 12 files (1 rewrite + 10 new subcomponents + 1 API extend + 1 cross-cutting export), +1,293 / -327. 3-column workspace-first layout (CRM-expert flipped Sadin's "symmetric with Contact 360" instinct — Account is a workspace container per PSA pattern, not a stakeholder record). Left = AccountSummaryCard (Building2 avatar + Active/Inactive + Primary Contact pill promoted from right column + Owner pill + 4 quick actions: + Project, + Contact, Email Primary, ⋯ More) + collapsible AccountKeyInfoSection with jump-to-tab buttons. Middle = AccountTabs with 5 triggers / 3 wired (Overview/Projects/Contacts; Activity + Billing disabled with `<Tooltip>` "Coming soon" wrapping `<span>` for the disabled-element workaround). Right = HealthSnapshotCard (project status-dot row using STATUS_COLOR + open leads count) + conditional OpenLeadsCard (renders null when count===0; cap 5 + "See all" link). **No Lead Provenance card** (dropped — lead-to-account lacks the 1:1 mapping lead-to-contact has). Backend extends GET `/api/v1/accounts/[id]` with `owner_email` (via `db.raw().auth.admin.getUserById`) + `project_status_mix` (6-key count reduced in app code, brief allowed in-app vs awkward PostgREST GROUP BY) + `open_leads_count` (PostgREST exact count with `head: true`) via parallel Promise.all. Cross-cutting: `PROJECT_STATUS_MAP` exported from `time-tracking/status-badge` as single label source-of-truth. One fixback caught at review (`de2f955`, rolled into the squash before merge): Sonnet had introduced friendly labels "Discovery"/"In Progress"/"Review" in 3 new files but `ProjectStatusBadge` renders "Planning"/"Active"/"In Review" — real same-page visual mismatch; fixback imports shared `PROJECT_STATUS_MAP[status].label`. Fixback also dropped a dead `ALL_STATUSES` runtime const (only used as a type — ESLint warning) and a dead `--dropdown-hover-overlay` CSS custom property. Build + ESLint clean (17 warnings, baseline). Brief archived. All 7 code-review items considered. **Lesson for brief catalog**: when a brief says "use design tokens" or "use status labels", be explicit about the source-of-truth file to import from — "use the design tokens" is too open-ended.
- 2026-05-29 PM — **Sidebar nav grouping shipped to stage** (`b6219b2`, squash from `feat/sidebar-nav-grouping`). 4 files, +186/-53. Introduces a 1-level-deep collapsible group pattern via discriminated union `SidebarEntry = SidebarItem | SidebarGroup` + optional `position?: "before-pipeline" | "after-pipeline"`. Applied to IT-agency: Projects + Time Tracking + Approvals nest under "Project Management" parent (icon `FolderKanban`). Pipeline repositioned to sit between Accounts and the new group via the position field — Pipeline itself wasn't moved out of UNIVERSAL_NAV_MIDDLE. `SidebarGroupRender` (inline) mirrors Public Forms collapsible UX with added `aria-expanded`. Default expanded; auto-re-expand via useEffect on `hasActiveChild`. Other 5 industries unchanged (back-compat via optional kind/position with sensible defaults). Counselor filtering: group drops Approvals child but stays. Brief archived. Build + ESLint local gates clean (17 warnings, same as before). All 7 code-review items N/A.
- 2026-05-29 — **`/contacts/[id]` 360° detail page shipped to stage** (`4890488`, squash from `feat/contact-detail-360`). 11 files (1 rewrite + 9 new subcomponents + 1 API extend), +1049/-269. 3-column Lead-detail-v2 parity layout: left = ContactSummaryCard + ContactKeyInfoSection; middle = ContactTabs (Overview wired, Notes/Activity disabled with "Coming soon" tooltips); right = ContactRelatedPanel (AccountCard → LinkedProjectsCard → RelatedContactsCard → LeadProvenanceCard conditional). Backend extends `GET /api/v1/contacts/[id]` with `source_lead` (lead provenance via reverse converted_contact_id lookup), `account_siblings` (LIMIT 11 to detect >10 cheaply), `account_owner_email` (via `scopedClient.raw()` escape hatch for auth.users). Built per CRM-expert review that flipped Sadin's proposed right-column ordering (account-first as the umbrella) and excluded several Lead-page features that don't translate to contacts (Stage, Convert, AI Insights, Score, Checklist, Log Meeting — all deferred or skipped). Page-padding fix applied preemptively (dropped wrapper's p-6 + max-w-3xl). All 7 code-review checklist items considered. Build + ESLint local gates clean. Brief archived. V2 surface listed in "Pick next" item above.
- 2026-05-28 late PM — **Production promotion shipped** (`f9af70d`, non-FF ort merge stage→main). Second prod promotion of the day. 7 stage commits land on main (3 chore/CI squashes + 4 docs): `/projects` Board chrome (`6de03ab`), `/projects` Board pipeline-parity (`2aa45df`), CI SSH `command_timeout` bump to 30m (`5ce03d2`), 4 docs commits. Deploy completed clean in ~1m48s with no concurrent stage deploys (the new pre-flight discipline held). Live smoke: `/login` 200, `/dashboard` + `/projects` + `/pipeline` + `/contacts` + `/accounts` all 307. New 30m timeout wasn't tested in this run (no contention) but is in place for future incidents. Two design briefs archived: `PROJECTS-BOARD-CHROME-BRIEF.md` + `PROJECTS-PIPELINE-PARITY-BRIEF.md`.
- 2026-05-28 PM — **Production SSH `command_timeout` bumped to 30m** (`5ce03d2`). 1-line `deploy.yml` change matching the value `deploy-staging.yml` has had since setup. Closes the 2026-05-28 PM `f78abcc` timeout incident. Promoted to production as part of `f9af70d`.
- 2026-05-28 PM — **`/projects` Board pipeline-parity shipped to stage** (`2aa45df`, squash from `chore/projects-pipeline-parity`). 5 files, +290 / -145. UI-only. Closes the remaining visual gaps from the first chrome brief: toolbar restructured into Pipeline's bordered-card shape with count chip + h-9 search + active-filters Badge; LayoutGrid icon dropped from title; full `ProjectCard` rewrite mirroring `LeadCard` (icon-square header + name Link + 3-dot dropdown / divider / Account/Contacts/Billable/Updated key:value grid / divider / urgency badge + owner avatar footer); whole-card-clickable via `useRouter`; columns widened 220→320 with Total/Billable footer; DragOverlay rewired through `<ProjectCard isDragOverlay />`. **Sonnet drive-by fix during the same branch**: page wrapper's `p-6` was double-padding `/projects` to 40px-from-edge vs `/pipeline`'s 16px (shell already provides `p-4`); dropped entirely. Promoted to a new code-review checklist item. Build + ESLint local gates clean (ESLint warnings dropped 18→17 because Sonnet cleaned up dead imports). Live smoke pending dev redeploy.
- 2026-05-28 PM — **`/projects` Board chrome shipped to stage** (`6de03ab`, squash from `chore/projects-board-chrome`). 3 files, +157 / -99. UI-only. Brings the IT-agency Board view's chrome in line with `/pipeline`: extends `FilterDropdown` with a discriminated-union `multiple` prop (13 existing single-select callers unchanged); collapses the blue project-status pill row into a multi-select Status FilterDropdown placed in Row 2; restructures `ProjectColumn` to mirror `PipelineColumn` (bordered header bar with colored status dot + name + count chip, bordered body with `bg-gray-50/40` fill, design-token `isOver` highlight, `FolderOpen` icon-in-circle empty state). New module-level `STATUS_COLOR` map: planning blue · active amber · in_review purple · delivered green · on_hold gray · cancelled red. Drive-by fix: `isActive` on `FilterDropdown` now also handles the `__all__` sentinel (previously caused default-state dropdowns using that sentinel to render as active). All 6 code-review checklist items N/A. Build + ESLint local gates clean. Brief archived. Live smoke pending dev redeploy.
- 2026-05-28 PM — **Production promotion shipped** (`f78abcc`, non-FF ort merge stage→main). All 10 stage commits land on production: Time Approvals nav (`683e85e`), 6 design-pass squashes (`285b2a8` `0d47bf3` `f3ad73d` `8791e66` `aec9cf5` `56f6299`), 3 docs commits. Live smoke clean: `lead-crm.zunkireelabs.com/login` 200, `/dashboard` + `/contacts` + `/accounts` + `/leads` + `/time-tracking/approvals` 307 (auth redirect). **First attempt hit the ~10-min SSH `command_timeout`** at the TypeScript step — a docs-only stage deploy was running concurrently, dual `npm ci` + `next build` slowed each build past the timeout. Recovery: waited for stage to finish, then `gh run rerun 26570173859 --failed` re-ran only the Deploy job (Pre-deploy Checks didn't repeat); success on retry in normal time. The incident is now in the ongoing-hardening section above + the resume block; permanent workflow fix deferred. See SESSION-LOG dated entry for full incident timeline.
- 2026-05-28 — **IT agency design pass — first wave shipped to stage** (6 squash commits on top of the Time Approvals nav, all UI-only). `/contacts` chrome aligned to `/leads` pattern (`285b2a8`); `/contacts` polished with avatar initials + Sort popover + client-side pagination using the `safePage = Math.min(currentPage, totalPages)` derivation pattern that sidesteps React 19's `react-hooks/set-state-in-effect` rule (`0d47bf3`); `--primary` CSS var swapped from `#2272B4` to `#171717`, all button sizes → `rounded-lg`, table data cells bumped to `text-gray-700 font-normal` (`f3ad73d`); table text hierarchy refined — name links → `#0f0f10`, secondary cells → warm-muted `#787871` (`8791e66`); FilterDropdown + PipelineSelector retoned, all blue accents removed, dropdown hover overlay set to `#0000170b` Anthropic-style (`aec9cf5`); `/accounts` Card-stack layout rewritten as a table mirroring leads + contacts (`56f6299`). All 6 code-review checklist items N/A across all 6 commits. Color tokens now established for future briefs (see SESSION-LOG entry for the full list). Six briefs archived. Production promotion pending Sadin's authorization.
- 2026-05-28 — **Time Approvals nav link shipped to stage** (`683e85e`, squash from `feature/nav-approvals-link`). `/time-tracking/approvals` was a built, prod-live admin page with no sidebar entry — reachable only from a stats card inside `/time-tracking`. Added an Approvals sidebar entry under Time Tracking, scoped to owner/admin only via a new optional `minRoles` field on `SidebarItem` (filtered in `getIndustrySidebarItems(industryId, role?)`). 5 files, 28/3. Pre-audit confirmed Approvals was the ONLY meaningful orphan in the IT agency sidebar; all other unlinked routes are correctly subpages or industry-gated elsewhere. Stage deploy 4m40s; live smoke /login 200, /time-tracking/approvals 307. All 6 code-review items N/A (UI-only). Brief archived. Set up the dashboard-by-dashboard styling/UX pass that's next.
- 2026-05-28 — **Production promotion shipped** (`d3cd235`, non-FF ort merge of stage→main). Deploy-to-Production green in 3m57s; `lead-crm.zunkireelabs.com` now current with Project Workspace v1 (all 5 phases + tag fixback + eslint hotfix) + dashboard chrome restyle (`#fafafa` chrome + white inset card) + Orca/Ops sidebar mode switcher + EdgeX brand wordmark. 19 stage commits, 41 files, 4159 / 133. Same merge pattern as the prior promotion (`c13e594`) — 3 main-only operational commits (prior promotion, Anish merge, CI nudge) blocked fast-forward; ort merge clean with zero conflicts. Live smoke: /login 200, /projects + /dashboard 307 (auth redirects, expected). Visual smoke deferred from stage and now applies to production. See SESSION-LOG.
- 2026-05-27 — **Project Workspace Phase 5 shipped to stage + CI red-streak reconciled** (`97e490d` Phase 5 squash + `04d7895` eslint hotfix). Phase 5 closes v1: keyboard shortcuts (`b`/`t`/`k`/`m` for views, `/` for search, Esc to clear), empty states with Clear-filters CTAs on all 4 views, a11y attributes (aria-label/pressed/sort/expanded across pickers + chips + columns + Members toggle), and the Phase 4 spec-gap fix (Assignee + Priority + Due filters now visible on Members view header; members-view body also narrows on assignee). **Discovery during ship**: stage CI had been red for 6 consecutive pushes since the tag-multi-picker fixback (`ed7ff15`) — `react-hooks/set-state-in-effect` hard error in `tag-multi-picker.tsx:39`. Local `npm run build` doesn't run ESLint, so the regression was invisible until CI exited 1 and skipped the container deploy. dev-lead-crm was frozen at Phase 3 the whole time. Hotfix wraps `setQuery` in `setTimeout(0)` mirroring the existing focus-on-open pattern. Deploy 4m4s; container jumped Phase 3 → Phase 5 + fix + all docs in one go. Live smoke: /login 200, /projects 307. **Visual smoke on tag fixback + Phase 4 + Phase 5 is now outstanding** — the earlier "tag picker fixback confirmed" entry was on a phantom (local or cached) since the deployed container never had the change. Brief moved to `docs/archive/features/PROJECT-WORKSPACE-BRIEF.md`. Production promotion blocked on smoke pass. See SESSION-LOG.
- 2026-05-27 — **Project Workspace Phase 4 shipped to stage** (`29345f3`, squash from `feature/project-workspace-phase-4`). Members view: one section per team member with ≥1 owned project OR ≥1 assigned open task. Aggregation is non-N+1 (≤3 total network requests). Section header with initials avatar + counts; collapsible body with read-only project + open-task rows; sort by busiest first. Workspace Owner filter narrows section list (single-person view). One brief-spec gap rolled to Phase 5: Members view header should also expose Assignee + Priority + Due filters per the brief — code APPLIES those filters but the header conditionals only render them on Tasks view. About 5 lines of conditional to fix in Phase 5. All 6 code-review items clean. See SESSION-LOG.
- 2026-05-27 — **Project Workspace tag-multi-picker fixback shipped** (`ed7ff15`, squash from `fix/project-workspace-tag-multi-picker`). Sadin's smoke caught that the original Phase 3 tag UX was naive free-text in both per-task and per-filter surfaces. Fixed with a unified Notion-style `<TagMultiPicker>` backed by a tenant-wide tag pool. New `GET /api/v1/tasks/tags` (FEATURES.PROJECT_BOARD gate, scopedClient, in-app dedupe). New `<TagMultiPicker>` (206 lines: search + autofocus + checkable list + Create-new-row when query doesn't match + case-insensitive duplicate guard + sm/md sizes). New `useTaskTags()` hook with refetch-on-PATCH-success propagation. TasksView's `handleTagAdd`/`handleTagRemove` collapsed into one `handleTagsChange` with optimistic update + revert-on-error pattern. Filter semantics unchanged (ANY-match via `.overlaps()`). All 6 code-review items clean. See SESSION-LOG.
- 2026-05-27 — **Project Workspace Phase 3 shipped to stage** (`867a750`, squash from `feature/project-workspace-phase-3`). Tasks view: shadcn Table with 8 sortable columns + inline edits (status/assignee/priority/due/tags) + "Log time" per row → pre-filled `<LogTimeDialog>`. New `GET /api/v1/tasks` cross-project endpoint (FEATURES.PROJECT_BOARD gate, counselor-scoped, paginated max 200, filterable by project/account/assignee/status/priority/tags/due/q). `PATCH /api/v1/tasks/[id]` extended with assignee_id + due_date + priority + tags (back-compat preserved via `"key" in body` for nullable fields). `<AssigneePicker>` (violet variant of OwnerPicker) + `<PriorityPill>` (colored pill dropdown) + `lib/due-keywords.ts` shipped as reusable components. LogTimeDialog/TimeEntryAddForm extended with optional `defaultTaskId`/`defaultProjectId` props (existing project-detail caller unchanged — verified via diff). All 6 code-review items clean. **Visual smoke pending** for Phase 3 (inline edits persist + tag chip add/remove + log-time pre-fill + counselor scoping). See SESSION-LOG.
- 2026-05-27 — **Project Workspace Phase 2 shipped to stage** (`dd20d91`, squash from `feature/project-workspace-phase-2`). Drag-and-drop on Board with TOCTOU `expected_status` precondition + 409 INVALID_STATE (mirrors time-entries approve pattern), card metrics (contact count via `project_contacts(count)` PostgREST embed with explicit FK disambiguation + billable hours via `/api/v1/time-entries/summary?dimension=project`), status multi-chip filter (rolled forward from Phase 1 P1 spec gap). All 6 code-review items clean. **One visual-smoke gap**: drag-and-drop wasn't verified in Playwright headless (known dnd-kit + CDP pointer event limitation; PATCH-level TOCTOU and chip wiring fully verified). Real browser will work — Sadin to visually confirm on `dev-lead-crm.zunkireelabs.com/projects`: drag card between columns + reload-persists + two-tab race shows 409 toast.
- 2026-05-27 — **Project Workspace Phase 1 shipped to stage** (`44409a8`, squash from `feature/project-workspace-phase-1`). Unified `/projects` with Board + Table views + lifted filters + URL-encoded state + admin-only gate. Migration 024 adds 6 columns + 6 indexes (tasks.assignee_id + due_date + priority + tags; projects.owner_id; accounts.owner_id). Both PATCH endpoints accept owner_id. `<OwnerPicker>` ready for `<AssigneePicker>` reuse in Phase 3. **One brief gap rolled to Phase 2**: status multi-chip filter (P1 spec) was missed by Sonnet; bundling into Phase 2 since the filter is most useful once drag-drop makes Board dynamic. Sonnet pre-emptively corrected a brief mistake (`authenticateRequest()` → `getCurrentUserTenant()` in the page shell) and surfaced it in the handoff — judgment-over-adherence behavior we want. Two spent feature branches deleted from origin. See SESSION-LOG.
- 2026-05-27 — **Production promotion shipped** (`c13e594`, non-FF merge of stage→main). Deploy-to-Production green in 4m22s; `lead-crm.zunkireelabs.com` now current with industry modules + Accounts + CRM Contacts v1 (Phases A–E) + Time Tracking v1 (Phases 1–5) + Anish's tags/contacts/lead-types + doc reorg. 173 files changed, 14,313 insertions / 448 deletions. Fast-forward was blocked by 2 main-only commits (`02fe74e` empty CI nudge + `e10b97d` Anish "Merge stage" from 2026-05-21) — both operational, no application code to preserve, no rebase needed; ort merge clean with zero conflicts. Live smoke: `/login` 200, `/dashboard` 200. Test residue from Phase E + Phase 5 smoke runs replicated to prod (cosmetic, known). Pre-flight surveyed Anish branches: 4 dangling zero-ahead branches and 1 stale unmerged branch found — none in flight, nothing to bundle. See SESSION-LOG.
- 2026-05-27 — **Time Tracking Phase 5 shipped to stage** (`f50f3ef`, squash from `feature/time-tracking-phase-5`). Rates + billable totals close out Time Tracking v1. `lib/rates.ts` (`resolveEffectiveRate(project, member)` with `project.default_rate → member.default_hourly_rate → 0` precedence) + `lib/totals.ts` (calculateBillableMinutes/Amount filtered to is_billable && approval_status='approved'; uses `rate_snapshot` not effective rate to preserve historical invoice integrity). Atomic `rate_snapshot` on approval: parallel fetch project + member rates → compute snapshot in app code → single atomic UPDATE writes status + rate_snapshot together, preserving the existing TOCTOU `.eq("approval_status", "pending")` precondition. New `/api/v1/team` PATCH plumbing for default_hourly_rate (admin-only, validates non-negative). Shared `RateInput` component (`$`-prefixed numeric) reused in `ProjectForm`; inline rate editor on `/team` gated to `industryId === "it_agency"`. New `/api/v1/time-entries/summary?dimension=member|project|account` endpoint with counselor scoping (non-admins query-filtered to own user_id + group-level safety net on dim=member). Billable totals UI surfaces: project-detail card (above Contacts section), approvals-queue projected billable per row, "Billable $" tile replacing "Entries" on home stats. Architecture decision affirmed: team PATCH is NOT industry-gated because `tenant_users.default_hourly_rate` is a universal-table column; UI gate is the meaningful gate. **First phase in this stretch with zero review fixbacks** — Sonnet's initial commit was clean on all 6 code-review checklist items first try. Opus re-ran 8/8 key invariants; 3 counselor/Admizz tests deferred to code-review only (auto-mode correctly blocked password rotation for re-test). Brief moved to `docs/archive/features/TIME-TRACKING-BRIEF.md`. Time Tracking v1 fully shipped; production promotion pending Sadin's go-ahead.
- 2026-05-27 — **CRM Contacts Phase E shipped to stage** (docs-only commit on `stage`; no feature branch — pure verification + doc sweep). Lightweight verification + doc sweep — zero application code shipped. 20-step smoke matrix run end-to-end (19 effective steps, Step 18 retired as redundant with Step 19): 3 visual via Sadin's screenshots (sidebar order, Admizz ProspectsView regression, tenant isolation spot-check); 13 API via custom Node harness as Zunkireelabs admin covering Phase B CRUD + Phase C junction + Phase D conversion + Phase D filter audit (`/leads`, `/accounts/[id]/leads`, `/pipelines` lead_count shape, `?include_converted=1` restore); 4 API via second harness as counselor (`/leads` scoped to assigned_to=self verified in DB; own-lead convert succeeds with `contact.assigned_to === counselor.userId`; convert on other's lead → 403) and Admizz admin (`/contacts` + `/accounts` + `/leads/[id]/convert` all 403, not 200/404/500). One informational finding (not a bug): counselor `GET /api/v1/contacts` returns all tenant contacts — no `assigned_to` filter, by design (admin gate on mutations; counselors are read-only viewers of contact roster). Matrix Step 15 wording was over-strict — the real counselor scoping invariant is on leads + convert, not the contacts list, and that invariant holds. Workflow held: verification + docs is Opus's job; no Sonnet code-writing was needed. Brief moved to `docs/archive/features/CRM-CONTACTS-BRIEF.md`. CRM Contacts v1 (Phases A + B + C + D + E) is fully shipped to `stage`; not yet promoted to main.
- 2026-05-27 — **GH Actions org-level suspension resolved** by GitHub support overnight. Two consecutive green Deploy-to-Staging runs (`6f067fd` 3m46s, `e1579b3` 3m48s) confirm the runner can pull again. `dev-lead-crm.zunkireelabs.com` is current with all of Phase A–E. No follow-up needed.
- 2026-05-26 — **CRM Contacts Phase D shipped to stage** (`35a5394`, squash from `feature/crm-contacts-phase-d`). Lead → Contact conversion live: `POST /api/v1/leads/[id]/convert` with TOCTOU-safe atomic update (same pattern as time-entries approve/reject — `.is("converted_at", null)` precondition + orphan contact cleanup on race-loss + 409). `ConvertLeadDialog` on `lead-detail-v2.tsx` (it_agency only) pre-selects existing account when `lead.account_id` set, create-new otherwise. NO_ACCOUNT sentinel + counselor scoping mirrored from lead + FK disambiguation. Cross-cutting `converted_at IS NULL` filter on 8 leads-fetching surfaces (queries.ts, leads list/bulk APIs, accounts/[id]/leads, both pipelines APIs) with `?include_converted=1` future-archive flag. One fixback (`11a3460`) caught at review for pipeline lead-count queries; new checklist item #6 added. Sonnet got all 5 prior checklist items right first try. Deploy still backlogged behind GH Actions incident.
- 2026-05-26 — **CRM Contacts Phase C shipped to stage** (`6dcbe6a`). Project↔contact junction wiring complete: 2 symmetric API routes (`/api/v1/contacts/[id]/projects` + `/api/v1/projects/[id]/contacts`) with TOCTOU-safe 23505→409 PRIMARY_TAKEN mapping, `ProjectContactPicker` shared component (pick-project + pick-contact modes), real Projects section on contact-detail, new Contacts section on project-detail (cross-feature touch from time-tracking). Cross-account links allowed (agency contractor reality, server-side warn log). One fixback for Radix Select empty-string sentinel — now item #5 on the code-review checklist. Sonnet pre-loaded Phase B's lessons (PostgREST FK disambiguation, optimistic-add shape match) without prompting — review checklist is paying off. Workflow held: fixback routed via Sonnet prompt, no Opus-direct edits.
- 2026-05-26 — **CRM Contacts Phase B shipped to stage** (`1909203`). Full Contacts CRUD + list + detail + ContactForm + ContactStatusBadge + account-detail integration with Primary Contact pill picker. Migration 022 closes the Phase A RLS gap. Three fixback commits caught: (1) PATCH email/phone invariant + q-param sanitization, (2) missing `/contacts/[id]` route shell + POST without join, (3) PostgREST embed ambiguity from the contacts↔accounts reverse FK. Four new code-review checklist items added (see above). Workflow violation noted: Opus did all 3 fixbacks directly instead of routing to Sonnet; memory updated to prevent recurrence.
- 2026-05-26 — **CRM Contacts Phase A shipped to stage** (`b622e5a`). Foundation layer: migration 021 (contacts + project_contacts tables, leads conversion columns, accounts.primary_contact_id), types extended, FEATURES.CRM_CONTACTS registered, sidebar entry above Accounts, `/contacts` shell now industry-dispatched (it_agency placeholder + education ProspectsView preserved). RLS gap on project_contacts caught at review — fix bundled into Phase B. See SESSION-LOG.
- 2026-05-26 — **Accounts promotion shipped to stage** (`13c528e`). Accounts is now a top-level CRM entity for it_agency with `/accounts/*` URLs, `FEATURES.ACCOUNTS` gate, Building2 sidebar entry above Time Tracking. 6 files moved with history preserved, 7 API routes re-gated, 2 intentional cross-feature imports introduced and documented. Tabs branch (`96fcaae`) deleted. Smoke verified as both Zunkireelabs (sees Accounts, /time-tracking/accounts 404s, /time-tracking-projects/[id] back-link goes to /accounts) and Admizz (no Accounts in sidebar, /accounts 404, API 403). Workflow incident — Sonnet's initial commit was missing 4 page-file edits that lived as uncommitted working-tree edits; fixed with additive commit on the same branch ("fix-back" pattern). See SESSION-LOG for lesson learned about checking `git status` before reviewing diffs.
- 2026-05-25 — **Time Tracking Phase 4 + 4.5 shipped to stage** (`d252568`). Phase 4: approvals queue + approve/reject API (atomic status precondition + audit + events). Phase 4.5: role-aware team timesheet table replacing the single-user home — admin sees all members with filters/stats/CSV export, member sees own entries scoped. Shared `useApproveReject` hook so both surfaces share the same 409 handling. Three commits (Phase 4, fixback for TOCTOU + timezone + edit-lock, Phase 4.5) rebased onto stage post-Anish-PR-#10 and merged as one. See SESSION-LOG.
- 2026-05-25 — **Accounts IA decision recorded** — promoting accounts to a top-level CRM entity (out of `/time-tracking/accounts/*` into `/accounts/*`) with a separate `FEATURES.ACCOUNTS` constant. Plan locked, Sonnet brief pending. The `feature/time-tracking-nav-tabs` branch (`96fcaae`) that proposed tabs-under-Time-Tracking will be discarded in the same refactor — Sadin pushed back that accounts isn't a Time Tracking sub-feature.
- 2026-05-25 — **Time Tracking Phases 1–3 shipped to stage** via the Opus-plans / Sonnet-executes workflow. Phase 1 (schema + manifest + 5 placeholder shells), Phase 2 (Accounts/Projects/Tasks CRUD), Phase 3 (time entries log + list + edit, with a timezone bug caught + fixed mid-review). Migration 020 applied live.
- 2026-05-25 — **Workflow split formalized**: Opus plans/reviews/pushes-to-stage; Sonnet executes feature code on per-phase branches; Sonnet never pushes to stage. Local-verify-before-push is the new flow (added mid-Phase-1, caught the timezone bug before it shipped).
- 2026-05-25 — **First IT-agency feature decided: Time Tracking.** Brief written at `docs/TIME-TRACKING-BRIEF.md`.
- 2026-05-25 — **Anish onboarded.** Prompt sent; he'll pull stage, read CLAUDE.md + `docs/reference/01-ARCHITECTURE-INDUSTRY-MODULES.md` + the migration playbook before starting his next feature.
- 2026-05-25 — **Anish's `view-details` branch adapted and merged into `stage`**. 3 commits (View Details panel, Student/Parent tags, tag selector in add form) cherry-picked onto the new industry-module structure. Git rename detection ported the check-in changes to the new file location automatically — zero manual conflict resolution. Migration 019_lead_tags.sql backfilled to close the schema-drift gap (Anish had applied the ALTER TABLE directly via MCP). Adapter branch and Anish's original branch both deleted.
- 2026-05-25 — **Architecture explainer + migration playbook added**. `docs/reference/01-ARCHITECTURE-INDUSTRY-MODULES.md` (visual old-vs-new comparison) linked from CLAUDE.md in two places. New CLAUDE.md subsection "Migrating an existing flat-pattern feature" gives a 10-step checklist for adapting old-pattern code. Together they make the onboarding path explicit so the next developer (or Claude session) doesn't have to derive the pattern from code archaeology.
- 2026-05-25 — **Code-review hardening pass**. RSC boundary bug (icon as function) fixed before any user hit it on prod; scopedClient cross-tenant escape closed (strips tenant_id from update/insert); auth.ts defensively handles tenants embed array vs object; getManifest(null) falls back to general; FeatureId typed in loader signatures. 11 of 15 review findings addressed; 4 low-severity items remain on the ongoing list.
- 2026-05-24 — **Industry module foundation (Path C) shipped**. `src/industries/` is now a first-class architectural concept with 7 industry folders, registry, loader, manifests. Student check-in and form-builder migrated into `industries/education-consultancy/features/`. Sidebar reads manifests (no more ternary). API gates added to all check-in and form-config routes (the check-in API was previously ungated for non-education tenants). CLAUDE.md restructured around the new pattern. `scopedClient(auth)` hardening wrapper added with proof migrations. See SESSION-LOG.
- 2026-05-24 — **PR #9 verification done** as part of the industry-modules work — form-builder was understood, migrated, and re-gated. The form-builder paths in CLAUDE.md are now accurate.
- 2026-05-24 — **Branch sync resolved** — rebased local onto `origin/stage` cleanly. The 4 prior local docs commits replayed without conflicts.
- 2026-05-24 — **SESSION-LOG backfill** written for the March–May shipped-work gap (PRs #4–#8 + commits `f728ca8` → `b890c35`).
- 2026-05-24 — Doc layout reorganized to match Stella+Zunkiree brain-folder pattern: SESSION-LOG.md as single source of truth, `reference/` for stable docs, `archive/<series>/` for shipped work.

---

## How this board works

- **🔴 Needs decision / action**: blocking items that require Sadin's input before a new session can move forward productively.
- **🟡 Open questions**: non-blocking ambiguities worth resolving when convenient.
- **✅ Recently resolved**: rolling log of items closed in the last few weeks — keeps the why-it-changed paper trail near where decisions were made.

Items here should be specific and actionable. If something becomes a multi-step project, write a `<CONTEXT>-BRIEF.md` at the top of `docs/` and link from here.
